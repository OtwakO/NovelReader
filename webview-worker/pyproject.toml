[project]
name = "novelreader-webview-worker"
version = "0.1.0"
description = "Private Patchright WebView sidecar for NovelReader"
requires-python = ">=3.13"
dependencies = [
  "patchright>=1.62.3",
]

[dependency-groups]
dev = []
