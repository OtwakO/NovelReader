import { beforeEach, describe, expect, it, vi } from 'vitest';
import { deleteReaderAccount, issueReaderPasswordReset, listReaderAccounts, setReaderEnabled } from './auth';
const request=vi.fn();vi.mock('./transport',()=>({request:(...args:unknown[])=>request(...args)}));
describe('account administration transport',()=>{beforeEach(()=>request.mockReset());it('unwraps ordinary reader accounts',async()=>{request.mockResolvedValue({accounts:[{id:'1',username:'Reader',status:'active',createdAt:1,updatedAt:1}]});await expect(listReaderAccounts()).resolves.toHaveLength(1);expect(request).toHaveBeenCalledWith('/auth/admin/readers')});it('uses strict encoded reader identities for lifecycle operations',async()=>{request.mockResolvedValue({});await setReaderEnabled('reader/id',false);expect(request).toHaveBeenLastCalledWith('/auth/admin/readers/reader%2Fid/status',{method:'PUT',body:JSON.stringify({enabled:false})});await issueReaderPasswordReset('reader/id');expect(request).toHaveBeenLastCalledWith('/auth/admin/readers/reader%2Fid/password-reset',{method:'POST'});await deleteReaderAccount('reader/id','Exact Name');expect(request).toHaveBeenLastCalledWith('/auth/admin/readers/reader%2Fid',{method:'DELETE',body:JSON.stringify({username:'Exact Name'})})})});
