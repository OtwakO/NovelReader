import fs from 'node:fs';
const initial=JSON.parse(fs.readFileSync('/tmp/explore-v12-initial.json','utf8'));
const rerun=JSON.parse(fs.readFileSync('/tmp/explore-v12-rerun.json','utf8'));
const sequential=new Map(rerun.targets.map(e=>[e.rawIndex,e.sequential]));
const diagnoses={
251:['source_incomplete_or_invalid','The exact mobile rank URL is live, but the source declares no mobile User-Agent. Legado and NovelReader both use a configurable desktop fallback when no header is declared; that desktop request redirects to a page outside the imported mobile selector contract. A mobile header makes the raw source work, but inferring it from the hostname would overfit and is not a Legado rule contract.'],
504:['source_incomplete_or_invalid','The imported category array is syntactically invalid at the `};{"title":"校园"` boundary. Production fails before request construction, while the intended API is HTTP 200 with 10 books.'],
617:['upstream_http','The exact first category repeatedly returns Cloudflare HTTP 521 (`Web server is down`); DNS is healthy and no source body reaches parsing.'],
274:['upstream_dns','The exact host `m.babahome.net` has no usable address; public resolvers report SERVFAIL/lame delegation, so no response body exists.'],
372:['site_drift','The exact catalog APIs are reachable but now return `{"code":1001,"message":"params exception","data":null}`. The imported script expects data arrays and fails against this changed API contract.'],
125:['source_incomplete_or_invalid','The live `/all/` page has 20 result rows, but the imported URL pattern does not match current `/txt/<kind>/txtNNNNN.html` links and its page-one route variants are stale. Removing only the invalid pattern allows extraction.'],
754:['upstream_dns','The exact host `www.frxsw.com` has no usable address; public resolvers report SERVFAIL/refusing delegated nameservers.'],
714:['audit_infrastructure','The audit host resolves `www.po18.in` only to `::`, causing local TLS failure. Public DNS returns 198.2.197.250; forcing that address yields HTTP 200 with 30 matching records, and the exact imported rules parse 30 books.'],
197:['engine_gap','The imported synthetic outer URL returns HTTP 400, which production rejects before its list rule can issue the real encrypted `java.ajax` request. The live Shubl API decodes to 15 books. With a 200 trigger, execution reaches additional missing reusable bridge APIs: `java.createSymmetricCrypto`, `java.base64DecodeToByteArray`, and `source.getLoginHeaderMap`.'],
355:['stale_source_contract','The exact category and route variants return HTTP 404, and the site root is itself a 404 page with no current catalog contract.'],
472:['site_drift','The current page has 30 matching result containers, but live book links moved from `/f/NNN.html` to `/NNN.html`; the imported URL pattern rejects every otherwise valid book.'],
740:['site_drift','Imported `/sort/0/1.html` is HTTP 404. Current routes use `/sort/0_1.html` and a different DOM (`.sort_box_list`/`.newbook_list`), so both route and selector contracts drifted.'],
910:['source_incomplete_or_invalid','The source is explicitly marked search-broken. The live page has 10 `.item` cards, while the imported rule selects absent `.category-div` and its URL pattern misspells `.co` as `.c`.'],
163:['engine_gap','The exact live page contains 20 `.video-box-inner` records. `class.video-play-icon@title` yields no names because arbitrary `title` is not recognized as a Default getter; equivalent `.video-play-icon@title` or `tag.a.0@title` rules return 20 books.'],
456:['site_drift','The old iQIYI comics category redirects to the generic homepage, which contains none of the imported fallback result structures.'],
224:['blocked_or_auth','The exact category is HTTP 403 without Referer. Adding only the site Referer returns HTTP 200 with 8 books, but the imported source does not declare that site-specific gate.'],
673:['source_incomplete_or_invalid','The live category contains 18 `.pic_txt_list` cards, but every imported `ruleExplore` field is blank, leaving no executable result contract.'],
877:['upstream_http','DNS resolves, but upstream TLS fails because the certificate is expired, yielding no usable HTTP response.'],
668:['upstream_dns','The exact host `www.wxdzs.net` does not resolve, matching both production transport failures.'],
156:['site_drift','The site now redirects to `www1.ikanbot.com` and exposes 24 current cards. The imported getter/pattern still assumes the old anchor context and `aikanbot.com/play/NNN`; updating both current contract pieces yields 24 books.'],
295:['stale_source_contract','The live page has 10 matching `.media` cards under `www.wxscs.com`, but the imported URL pattern still requires the obsolete `www.wxsc8.com`; changing only that hostname yields 10 books.'],
17:['engine_gap','The live page has 10 matching cards. Valid Legado singleton indexing `a[1]@href` is unsupported because production recognizes only bracket ranges containing `:`. The name rule also hits a blank first `a@title` and fails to skip to the later non-empty attribute; equivalent supported index plus explicit nonblank name yields 10 books.'],
633:['source_incomplete_or_invalid','The imported category script has invalid destructuring syntax `orList.map([tag,ure]=>...)` and throws `Malformed arrow function parameter list`; its intended API is healthy with 12 items.'],
};
function compact(run){if(!run)return null;const c=run.catalog,p=run.page;return{catalog:{status:c.status,durationMs:c.durationMs,entryCount:c.body?.entries?.length??0,error:c.status===200?null:{code:c.body?.code,stage:c.body?.stage,severity:c.body?.severity,retryable:c.body?.retryable,message:c.body?.message}},category:run.category,page:p?{status:p.status,durationMs:p.durationMs,books:p.bookCount,distinctBookUrls:p.distinctBookUrls,exhausted:p.exhausted,diagnostics:p.diagnostics,samples:p.sampleBooks,error:p.status===200?null:{code:p.body?.code,stage:p.body?.stage,severity:p.body?.severity,retryable:p.body?.retryable,message:p.body?.message}}:null}}
const entries=initial.entries.map(e=>{const authoritative=sequential.get(e.rawIndex)??e.initial;const run=compact(authoritative);const pass=run.page?.status===200&&run.page.books>0;const diagnosis=diagnoses[e.rawIndex];const [classification,evidence]=pass?['credible_nonempty',`Authoritative production Explore returned ${run.page.books} books with ${run.page.distinctBookUrls} distinct book URLs${run.page.diagnostics?.length?' and an explicit bounded-result diagnostic':''}.`]:diagnosis??[];if(!classification)throw Error(`missing diagnosis ${e.rawIndex}`);return{rawIndex:e.rawIndex,bookSourceUrl:e.sourceUrl,name:e.name,rank:e.rank,initial:compact(e.initial),sequential:sequential.has(e.rawIndex)?compact(sequential.get(e.rawIndex)):null,classification,evidence}});
const counts={};for(const e of entries)counts[e.classification]=(counts[e.classification]??0)+1;
const credible=entries.filter(e=>e.classification==='credible_nonempty');
const gapProbes={
251:{family:'Mobile source contract receives desktop default User-Agent',url:'https://m.ac.qq.com/rank/index?type=rise&pageSize=10&page=1',desktop:{finalURL:'https://ac.qq.com/Rank/comicRank/type/rise',httpStatus:200,importedSelectorMatches:0,desktopRankRows:30},mobile:{finalURL:'https://m.ac.qq.com/rank/biaosheng',httpStatus:200,matchingComicLinks:7,productionBooksWithHeaderOnly:7}},
197:{family:'Rule-driven AJAX is blocked by outer non-2xx and required crypto/login bridge APIs are absent',outer:{url:'https://www.baidu.com/category_type=1&order=week_click&page=1',httpStatus:400},inner:{url:'https://app.shubl.com/bookcity/get_filter_search_book_list',httpStatus:200,decodedBooks:15},missingAPIs:['java.createSymmetricCrypto','java.base64DecodeToByteArray','source.getLoginHeaderMap']},
163:{family:'Default class traversal does not recognize arbitrary attribute getters',url:'https://www.missevan.com/explore/538?p=1',matchingCards:20,rule:'class.video-play-icon@title',productionBooks:0,equivalentRules:[{rule:'.video-play-icon@title',books:20},{rule:'tag.a.0@title',books:20}]},
17:{family:'Default singleton bracket indexes and blank-skipping multi-match attributes are unsupported',url:'https://seqing001.com/?cat=2',httpStatus:200,matchingCards:10,rules:{bookUrl:'a[1]@href',name:'a@title'},runtime:{exactBooks:0,supportedIndexOnlyBooks:0,supportedIndexAndNonblankNameBooks:10}},
};
const audit={schemaVersion:1,auditedAt:new Date().toISOString(),seed:initial.frozen.seed,corpus:initial.frozen.corpus,selection:initial.frozen.selection,execution:{method:'Fresh isolated SQLite database; unmodified corpus imported through production API; catalog then first selectable URL category page 1.',initialConcurrency:4,timeoutSeconds:90,sequentialConfirmation:'Every initial catalog/page failure, empty result, duplicate-only result, timeout, or diagnostic was rerun sequentially against the same fresh environment; sequential result is authoritative.',sourceOrParserChanges:false},summary:{sampleSize:entries.length,credibleNonempty:credible.length,totalDistinctBooks:credible.reduce((n,e)=>n+(e.sequential?.page?.distinctBookUrls??e.initial.page?.distinctBookUrls??0),0),counts,sharedEngineGaps:[{family:'Outer non-2xx rejection prevents rule-driven AJAX',rawIndices:[197]},{family:'Missing crypto/login Java bridge APIs',rawIndices:[197]},{family:'Default class traversal does not recognize arbitrary attribute getters',rawIndices:[163]},{family:'Default singleton bracket indexes are unsupported',rawIndices:[17]},{family:'Multi-match attribute extraction does not skip blank leading values',rawIndices:[17]}]},gapProbes,entries};
fs.writeFileSync('testdata/booksource/audits/explore/explore-live-audit-v12-2026-07-31.json',JSON.stringify(audit,null,2)+'\n');
console.log(audit.summary);
