---
status: in-progress
updated: 2026-09-17
---

# EPUB support: structured novel reading

## Goal

Import a DRM-free reflowable EPUB, review it, add it to the existing shelf, read its text and illustrations with meaningful formatting/navigation, resume and bookmark it, and back it up or remove it through the existing reader-owned lifecycle.

This is a structural milestone, not a second reader application. Done means a reliable, explicitly bounded novel-reading profile, not universal EPUB reading-system conformance.

## Accepted direction and authorization

The user selected structured reflowable EPUB 2/3 reading and both browser and server-inbox import. After reviewing the proposal and reference research, the user authorized implementation, starting with the bounded contract proof. Keep existing reader homes/backups untouched. The user approved epoch-14 storage and subsequently epoch-15 publication/reading integration on fresh isolated data under the no-migration policy. Existing epoch-13/14 homes/backups remain untouched. Epoch 15 is now implemented with publication/reading integration; earlier parser/projection checkpoints did not change the schema.

Follow [reading documents/resources decision](../decisions/0002-reading-documents-and-resources.md). Reuse the library, reading session and prose modality. Do not create a plugin framework or an EPUB-specific reading stack.

## Scope and why

### Include

- Browser uploads and explicit server-inbox selection within Local Import; mixed TXT/EPUB selections retain independent outcomes.
- Package metadata, embedded cover, ordered reading sections, EPUB 3 navigation and EPUB 2 NCX navigation.
- Paragraphs, headings, emphasis, explicit line breaks, lists, quotations, illustrations/alternative text, internal links and footnotes. Include ruby annotations and simple semantic tables in the content-contract checkpoint: these carry meaning in novels and should not be silently concatenated into plain text. No CSS layout emulation.
- Existing typography, image visibility, Chinese conversion, progress, last-read tracking, bookmarks and revision-qualified navigation.
- Default-on review preference; disabling it may automatically accept only an eligible warning-free initial import from current client work, never a recovered receipt or a degraded book without approval.
- Review metadata, contents and a bounded sample; clear unsupported-content warnings; explicit acceptance, retry/discard and persisted history.
- Reader isolation, crash recovery, bounded resources, portable backup/restore and complete removal/cleanup retry.

### Exclude

- DRM decryption, fixed-layout/mixed fixed-layout publications, publisher CSS/fonts, scripts, forms, audio/video/media overlays, full SVG/MathML rendering, vertical-layout fidelity and exact printed pagination.
- EPUB editing/export, replacing a published original, published reparse, version history, cross-provider merging, content-hash deduplication, full-text search and downloadable offline packages.
- EPUB CFI or a universal location framework. Existing section/progression persistence remains the default; exact internal-link targets are a separate navigation concern.
- Automatic remote resource retrieval, archive extraction trees, a rendering browser service or client-side archive parsing.
- User-configurable cover overrides. The user left this as a possible later shared-library feature across all providers, not part of EPUB implementation.

These exclusions narrow fidelity, not data ownership: preserve the original unchanged. State the supported profile before upload/review; do not advertise complete EPUB compatibility.

### Direction assessment

**Why not text-only conversion?** It would be cheaper initially, but lose emphasis, reading order distinctions, notes and intra-document navigation. Repairing that after publication could change locations and require reinterpretation. It does not satisfy the selected scope.

**Why not embed a full EPUB browser reader?** It can preserve publisher layout better, but adds CSS/DOM isolation, archive/resource delivery and a separate navigation/location lifecycle. That is justified for publisher fidelity, not this novel-focused milestone.

**Why both intake channels?** They are two ways to acquire the same durable original, not two parsers. Browser-first is a useful implementation checkpoint, but server-inbox parity belongs in milestone completion. Reuse existing inbox safety and explicit cleanup proof; do not implement a second independent intake scheduler.

**Main cost:** semantic documents and navigation, plus lifecycle integration. ZIP/XML decoding alone is not the milestone. The first checkpoint must exercise these costs before committing to detailed persistence.

### Reader references and performance

The user clarified that original publisher layout is unnecessary and efficiency/performance matter. [Legado/web-legado/Readest findings](../notes/2026-09-17-epub-reader-reference-lessons.md) support app-owned responsive typography while preserving semantics. Readest's polished reading layout does not require adopting its Foliate browser-rendering stack; Readium Go was evaluated separately as a package-parsing candidate.

- Prepare and persist semantic sections once, processing one section at a time. Ordinary reading must not download, inflate, parse or mount the whole book.
- Smooth interactive reading is an explicit implementation priority: keep expensive preparation off request/reader interaction paths, reuse bounded shared loading/prefetch, and keep resource delivery independent of section text. Measure actual bottlenecks before optimizing; retain clear ownership rather than adding speculative cache layers or format-specific scheduling. Full pixel validation belongs to preparation, not repeated resource delivery; durable validated evidence and immutable originals must make that boundary trustworthy.
- Keep images separate and lazy; preserve validated dimensions where available to reduce layout shift. Reader style changes reflow local content, not trigger reimport.
- Reuse the shared chapter loader's request deduplication and bounded recent cache/prefetch. Check whether rich-section byte limits or byte-weighted retention are needed; do not create an EPUB-only cache owner.
- Include a large single-spine document in the first proof. A section can contain much more than a conventional chapter; do not assume five cached sections means small memory, or add speculative splitting/virtualization before measuring.
- Preserve the current reading mode. Use fluid single-column phone text and comfortable desktop measure; no new full-book pagination, dual original/parsed modes or unrelated reader redesign in this milestone. Borrow Readest's quiet controls and contextual navigation, not its entire feature set.
- Record import time/peak memory, prepared size, cold/warm section payload/load and image layout movement on a small normal fixture and a large-section/image-heavy fixture. These focused observations inform limits and parser choice; no benchmark framework or unmeasured speed claims.

## Evidence from the current code

- `backend/internal/reading/service.go`: a private provider interface already separates catalog/open/location lookup; `resolve` chooses BookSource or TXT. Add one concrete EPUB adapter here, not registration machinery.
- `backend/internal/reading/document.go` and `frontend/src/api/reader.ts`: current backend providers still emit version 1 (paragraph/image blocks). The frontend transport now strictly admits both versions after cross-language and composed-browser proof; no EPUB provider is exposed.
- `frontend/src/features/reader/ProseRenderer.vue`: one app-owned semantic renderer now serves legacy adapters and candidate structured documents; no publisher HTML or second EPUB renderer.
- `frontend/src/features/reader/chinese-conversion.ts`: pure conversion now supports structured text without changing canonical documents or identifiers/targets. The shared loader/memoization path handles both supported document versions.
- `backend/internal/library/schema.go`: shared progress/bookmarks use chapter index, normalized position and content revision. No evidence yet requires replacing that persistence model.
- `backend/internal/fileimport/pool.go` and `work.go`: the former `txtimport` lifecycle owner has moved without changing scheduling behavior. Concrete dispatch/recovery still process TXT only; integrate EPUB here, not through a second pool or changes to TXT's parser/state tables.
- `backend/internal/txtstore/receive.go`, `portable.go`: acquisition is distinct from publication; originals and durable intent participate in the reader-home mutation/snapshot lifecycle. Portable validation does not rebuild content.
- `frontend/src/features/imports/import-queue.ts`: current orchestration and DTOs are TXT-specific. Share queue lifecycle with typed format-specific operations rather than copying the queue or making every receipt carry TXT options.

Format checks against [EPUB 3.3](https://www.w3.org/TR/epub-33/): [spine itemref](https://www.w3.org/TR/epub-33/#sec-itemref-elem) distinguishes primary and auxiliary reading; [TOC navigation](https://www.w3.org/TR/epub-33/#sec-nav-toc) is a hierarchy of targets, not one entry per spine item; [font obfuscation](https://www.w3.org/TR/epub-33/#sec-font-obfuscation) is distinct from DRM. EPUB 2 package/NCX rules still need focused primary-source checking during the contract checkpoint.

## Accepted architecture

### Ownership and flow

```text
Local Import (browser / inbox)
  → shared bounded intake and worker lifecycle
  → EPUB-owned durable receipt + unchanged original
  → bounded EPUB parser/normalizer
  → saved sections, navigation, resource/anchor mappings and diagnostics
  → explicit library publication transaction
  → existing reading service + EPUB adapter
  → versioned semantic prose + authorized opaque resources
  → shared Reading Session / Prose Renderer
```

- `internal/epub`: bounded container/package inspection, URL resolution and semantic normalization; no HTTP, library mutations, home paths or worker ownership. **Selected after the build proof:** focused EPUB rules over Go ZIP/XML and existing HTML/text primitives, with no new dependency. Readium's package-level coupling brings unrelated cloud/gRPC/telemetry dependencies even through its narrower exported parsing functions. The user chose the focused Go approach after reviewing that measured cost. Do not fork Readium, maintain parallel parsers or expose archive references in HTTP/storage-facing prose documents. Format-rule ownership and conformance tests are the cost of this choice.
- `internal/epubstore`: original, durable import state, prepared content/index, publication and provider-owned removal/recovery/portable validation. Library alone owns display metadata, progress, bookmarks and revisions; parsed metadata before admission is import evidence, not a second authoritative shelf record.
- Extend the promoted `fileimport` lifecycle owner as EPUB lands. One shared process budget and reader quiescence barrier; concrete TXT/EPUB dispatch with fair selection so continuous TXT work cannot starve EPUB. No dynamic registry, generic task engine or extra independent worker pool.
- Share confined transfer/finalization and inbox claim mechanics only where both formats have the same invariant. Keep encoding/regex/reparse and archive normalization in their respective owners. Do not rename/rebuild every TXT table as a prerequisite.
- HTTP owns authentication, DTOs and safe response headers. Reading service selects the provider. Renderer receives semantic content, never ZIP paths, OPF metadata or provider parsing instructions.

### Sections, TOC and footnotes

1. Main sequence follows the package spine, not ZIP order, manifest order or TOC order. One supported spine content document is one section initially; no heuristic splitting by headings.
2. Navigation is distinct: preserve hierarchy and map entries to section plus opaque anchor. Several entries may target one section. Extend catalog output with optional navigation rather than duplicating sections or overloading `isVolume`.
3. Normalize document IDs to opaque anchor keys during preparation; links carry validated publication-local targets qualified by the same content revision. No archive href reaches the client.
4. Auxiliary/non-linear and referenced note documents remain reachable but do not become ordinary next/previous or prefetch steps. Accepted representation: indexed sections with an optional `auxiliary` flag; omission means main membership for existing catalogs. Main order is catalog/spine order with auxiliary sections skipped, never an ordinal prefix. Keep section ordinals stable within the published interpretation.
5. Use ordinary note navigation with a visible return action before considering popovers. The Reading Session retains a step-by-step return stack for nested note visits, including notes within a main section; returning restores the prior visit's progress eligibility and location. Ordinary deliberate chapter navigation exits note context. Do not create a second independent reader for notes. Auxiliary views must not overwrite the main resume/last-read position. Accepted: allow auxiliary bookmarks and reopening them, while auxiliary reads and bookmark operations must not overwrite main resume/last-read. Test that behavior in the first checkpoint rather than accidentally accepting indices that shared validation cannot restore.
6. Keep normalized progression for ordinary resumes/bookmarks. An anchor jump resolves after document display, then ordinary main-section progress can be saved. No claim of exact text-position restoration after typography changes. A persisted anchor locator is a separate decision only if the checkpoint demonstrates a real failure of this contract.
7. Missing optional TOC: generate a clearly labeled section list from the spine with review warning. Broken individual targets remain visible as unavailable or produce a review diagnostic; never silently redirect to another chapter. Missing primary readable content is a failure, not a successful partial book.

### Semantic prose and assets

- Define a finite prose schema: supported block types and a small inline vocabulary (text, emphasis, links, ruby, line breaks). Lists/tables have bounded semantic structure, not arbitrary tags/attributes/styles.
- Proposed wire compatibility: add version 2 for richer documents; retain version 1 BookSource/TXT parsing while both are served. Do not silently send new blocks under version 1. Frontend parsing and renderer cases are exhaustive; unsupported versions fail explicitly.
- Preserve language/direction where needed for correct text semantics. Publisher font metrics, generated CSS content and visual placement are not preserved. Unsupported meaningful content yields diagnostics, not silent disappearance; meaningful fallback text remains readable where available.
- Render Vue-owned elements and text; no raw publisher HTML via `v-html`. Conversion visits text leaves/labels without changing opaque IDs or target references. Links/buttons must not trigger reader tap-navigation accidentally.
- Issue opaque publication/revision-bound image and cover resources. Authorization and removal/restore lifetime checks remain shared; archive lookup is EPUB-owned. Validate media type and byte/dimension bounds; never serve arbitrary archive contents as same-origin active content.
- Start with supported raster images. Do not ship a general SVG sanitizer/rasterizer. Common SVG wrappers containing a supported image can normalize to that image only through a narrow parsed rule; unsupported standalone vector art is reported. A cover failure need not make readable prose unusable.
- No remote fetch for referenced publication resources. Accepted: retain HTTP/HTTPS external links for deliberate opening in a separate tab with opener isolation; never fetch them in preparation or prefetch. Other schemes remain inactive. Transport/renderer integration must enforce this policy.

### Shared image processing and optional EPUB optimization

This supersedes the earlier original-only milestone decision, not the requirement to retain the original EPUB. Performance work belongs in preparation; ordinary reading never triggers image conversion.

**Product contract**
- Local Import offers `Original` (default) and `Optimized` for EPUB before files are queued. Snapshot the mode into each import attempt for both browser and server-inbox intake; changes affect only newly queued EPUBs and never TXT. No global settings page, quality slider or live reader toggle initially.
- Optimized means static **8-bit WebP, quality 92**, with a **2048-pixel longest-edge cap**. Resize proportionally only when the oriented width or height exceeds the cap; never upscale, crop or stretch. Smaller images retain their dimensions but are still encoded as WebP. Retain alpha transparency. Original mode preserves encoded bytes and the existing orientation-aware display dimensions.
- Keep the unchanged original EPUB in both modes. Prepared text/navigation are shared by either mode, not duplicated by image policy. Review shows the chosen mode and available aggregate derivative size/count; image-mode changes after preparation/publication require reprocessing and are out of scope. Custom cover UI and post-publication image switching are deferred.
- The 32 Mi-pixel source admission limit remains: downscaling does not bypass source byte/pixel validation or remove its decoding-memory peak. WebP quality is an encoder setting, not a fidelity percentage; resizing/lossy encoding can lose detail, particularly when zooming. Neither a smaller encoded file nor identical appearance is guaranteed for every image.

**Module boundary and dependency direction**
- Introduce `internal/imageproc` as a concrete, stateless backend module. It owns bounded static-raster validation, decoding, orientation, aspect-preserving resize and encoding. It does not import EPUB, HTTP, library/storage, jobs or filesystem-path owners. No global mutable state, plugin registry, processor interface with one implementation, or configurable backend selector.
- Small public surface: `Validate(ctx, encodedBytes, declaredMediaType) -> Info` and `Optimize(ctx, encodedBytes, declaredMediaType, Profile) -> Result`. `Info` describes safe media type and display dimensions; `Result` adds encoded output bytes. Profile expresses the actual required quality and maximum edge, with byte/pixel limits explicit at the boundary; fixed EPUB values are owned by the EPUB caller. Final Go names/types are implementation details, not a new public HTTP contract.
- Share private decode/validation primitives. `Optimize` performs its own validation and decodes once; callers must not run full `Validate` first. Move the current EPUB image/orientation logic and relevant synthetic tests into this owner rather than keeping a second implementation. EPUB translates shared typed failures into existing admission errors/diagnostics; codec errors and paths never reach clients.
- Optimized output bakes orientation into pixels, reduces precision to 8-bit, and emits matching dimensions without stale orientation metadata. Check alpha edges and color-profile handling in the encoder proof; do not claim color-managed fidelity without verified support. Existing unsupported animation/vector behavior is unchanged. No external image URLs or user-provided codec commands.
- Backend identity is immutable process capability, not a user-controlled profile setting. Original validation must not depend on native codec availability. Optimized imports may use the portable fallback, but the lifecycle owner records that fact and the frontend visibly explains the slower preparation; Docker verification must still require native encoding in the supported release image.
- Future custom-cover uploads can call the same module with a cover-specific profile while owning upload authorization, storage and original-retention policy themselves. Do not implement cover-specific profiles, endpoints or lifecycle now.

**EPUB preparation, storage and delivery**
1. Extend the existing preparation boundary with the immutable image policy and a caller-owned derivative output callback. `epub` owns archive references, resource discovery and section binding; `imageproc` owns pixels/codecs; `epubstore` owns staged files, attempt identity and durable resource mappings. No storage paths enter the processor or public prose schema.
2. Resolve each referenced resource once per preparation, including a cover reused in prose. Extend the existing resource-key metadata cache: original mode validates once; optimized mode validates/decodes/transforms once and immediately emits its bytes to attempt staging. Cache only metadata and the staged resource identity, not decoded images or all encoded outputs. Process images sequentially within the shared bounded import worker; do not introduce another pool or whole-book image preloading.
3. Persist the selected mode/profile version with the attempt. Store a private per-attempt resource record identifying original archive entry versus prepared derivative, media type, actual dimensions and size. Bound per-image and aggregate derivative output bytes before accepting staged writes; choose named limits at the storage checkpoint rather than assuming compressed sources bound generated storage. Use the same opaque revision-qualified authorized resource route for both. Bind prose and selected cover dimensions to the selected reading resource, preventing image-load layout shifts. No runtime conversion, archive reparsing or repeated pixel validation on ordinary resource requests.
4. Finalize resources with prepared sections/navigation under the existing generation guard. Output is not visible until the exact attempt completes; retries cannot append duplicate derivatives or reuse another attempt's output. Failure/cancellation discards incomplete attempt output; stale work cannot publish. Keep original/review metadata and derivative mappings consistent rather than falling back silently to originals when optimization fails.
5. Carry owned derivative files/mappings through removal, shutdown/restore quiescence, and portable backup validation. Backups include the actual prepared reading assets so restore does not re-encode under a different codec version. Validate required assets and references before publishing restored data. Missing/corrupt derivative handling must be explicit; avoid a second original-fallback delivery path that hides damaged state.

**Implementation sequence and verification**
1. Prove one Go WebP encoder and resize path before adding a production dependency: inspect maintenance/license, native/WASM/Go footprint, actual supported build targets, Alpine/container linkage, cancellation behavior, alpha/orientation/color handling and bounded outputs. The existing container already uses CGO for OpenCC, but that does not make a WebP dependency cost-free. Do not assume Go is faster than the Python/libwebp comparison, or that quality 92 is identical across encoders. Surface material deployment tradeoffs before selection.
2. Measure the actual Go pipeline (decode + orient + resize + encode) on a few synthetic image cases, including unchanged-small and capped-large dimensions, transparency and a 16-bit source. Record time and peak allocation separately from client memory. Observe cancellation between stages; do not pretend a context interrupts an uninterruptible native codec call. Use existing source bounds and shared worker ownership rather than per-image goroutines, forced GC or a speculative memory scheduler.
3. Implement the shared module and adapt EPUB validation first with original-mode behavior preserved. Then add optimized preparation and resource emission, followed by durable lifecycle integration and import UI; do not expose a toggle until the entire path works. The separately authorized epoch-14 storage/lifecycle cutover is implemented; the import toggle remains unexposed.
4. Focus tests on the real boundary: no upscale/proportional cap, orientation + alpha + emitted dimensions, original-byte preservation, single preparation per repeated resource, cleanup/generation failure, and one portable/resource round trip. Use small synthetic fixtures and the existing test owners. Verify the real reader receives prepared images with correct reserved geometry and makes no conversion request; low-end-device responsiveness remains a measurement goal, not a guaranteed result from encoded-size savings.

### Storage and lifecycle

- Keep one immutable `original.epub` under the reader's managed files and disposable intake work separately. Read validated ZIP entries directly; do not extract arbitrary archive paths into a directory tree.
- Persist normalized section documents plus navigation/anchor/resource mappings during preparation. This makes published semantics stable across restarts/parser updates and avoids whole-book parsing on each chapter request. Original image bytes stay in the archive; do not extract a second set of originals. The accepted optional optimization design above adds only selected reading derivatives, owned by the same preparation attempt. Both modes keep server-side parsing, preparation and image validation off the reading interaction path; clients decode whichever validated reading resource was selected.
- EPUB owns a small receipt/result model, not TXT's active/candidate reparse model. Allocate an attempt generation before work; a completed index and diagnostics become visible atomically. Publication uses the exact reviewed attempt and is idempotent. Published content is immutable in this milestone.
- Receiving, queued/processing, ready/review-required, failed, published and removing are lifecycle concepts. Storage tables and legal receipt/preparation states are defined in `epubstore/schema.go`; ready is not publication or review approval.
- Long archive parsing happens outside SQLite write transactions and mutation gates. Finalization/publication use short guarded transactions; canceled or superseded work cannot install results. Incomplete prepared rows/files remain hidden and have explicit cleanup/recovery ownership.
- Recovery runs only while quiescent. Interrupted EPUB running attempts fail for explicit retry; queued attempts remain queued, and recoverable installed finalizations are completed. TXT retains its existing interrupted-analysis recovery policy. Removal hides the publication and retires work before deleting owned bytes; incomplete cleanup is observable/retryable. Restore, deletion and shutdown drain transfers/work/resources under existing reader-home ownership.
- Backups include original and authoritative prepared content/mappings consistently. Validate bounded stored documents/references and required originals, without re-normalizing an EPUB or running content during restore. Do not export inbox cleanup authority. Pending/failed/removing states need their own legal missing-file rules.
- The authorized reader epoch **14** registers EPUB persistence and shared worker lifecycle. Retain old homes/backups untouched and verify fresh isolated homes; no migration, automatic reset or deployment is authorized. Rollback uses the prior application with its compatible epoch-13 home; never open epoch-14 data with the old app.

### Shared import scheduling

Reuse the existing import lifecycle as `internal/fileimport`, rather than adding an EPUB pool or a generic job framework. Keep **two preparation workers and two independent transfer slots process-wide**, one active preparation per reader, and the existing round-robin turns between readers. Select the oldest durable queued work across TXT and EPUB within each reader; format-specific selection/claim/preparation and recovery remain storage-owned.

Do not increase concurrency without measured queue pressure, memory headroom and reading responsiveness under concurrent imports. Idle workers have small overhead, but simultaneous image-heavy EPUB preparations can materially increase peak memory. No capacity claim for hundreds of concurrent imports is established.

The lifecycle-owner move and epoch-14 storage integration are complete, including store-owned pending-work selection/generation claims, EPUB dispatch/recovery and mixed-format lifecycle tests. Epoch 15 adds publication/reading integration below; no missing-table fallback, optional parallel pool or runtime feature switch. Verify only with fresh temporary homes and reject epoch-13 homes/backups without conversion; rollback uses the prior binary and preserved epoch-13 data. Keep recovery before admission, and cancel/join both transfers and preparation before restore, deletion or shutdown. This refactor is reversible in code with no data conversion; epoch-13 homes remain untouched.

### Practical input protections

EPUB is an untrusted archive, so these are necessary boundary checks, not a general security overhaul:

- Bound compressed input, central-directory entry count, total declared and actually read uncompressed bytes, per-entry bytes, document/nesting/node counts, sections and resources; enforce cancellation/deadlines while decompressing/parsing, not just ZIP header claims.
- Reject ambiguous duplicate normalized names, escaping/absolute paths, unsupported encryption/compression and broken required references. Resolve URLs relative to their owning package/document; allow legitimate internal `../` references only when the resolved path stays within the archive. Decode consistently before lookup; fragments are not entry names.
- Do not resolve external XML entities/DTDs or fetch schemas. Handle normal EPUB 2 declarations using a bounded parser policy, not network access.
- Do not classify every `encryption.xml` as DRM: ignored obfuscated fonts do not require decryption. Encrypted required reading content is unsupported; expose a safe category.
- Validate the package's fixed-layout declarations and spine overrides, not only the filename. Unsupported primary content fails or requires a supported declared fallback; do not silently skip primary sections.
- Stable client error categories and bounded diagnostics; raw paths/parser details stay out of public responses. Numerical limits are named constants chosen at the first checkpoint, not new knobs for every theoretical case.

## Delivery checkpoints

1. **Contract proof, before broad plumbing — in progress.** Dependency gate complete: Readium compiled successfully, but the user selected focused Go parsing after its actual package graph was measured (see Current State). Bounded archive/XML, package inventory/navigation inspection, package support checks, isolated section normalization, static image binding validation and registered-section target binding, staged preparation and embedded cover selection are implemented; the complete parser-to-reader design is not yet accepted as proven. Initial preparation measurements include a large single-spine document; browser behavior and worst-case image costs still need proof. Build minimal synthetic EPUB 2/3 fixtures containing a main sequence, multiple TOC anchors in one file, cross-document notes and illustrations. Validate parser/semantic document/renderer/navigation together. Freeze the version-2 schema, section/TOC/auxiliary rules, semantic support matrix, limits and exact failure categories. Check EPUB 2 primary references. If this requires a fundamentally different location or renderer model, stop and revisit scope rather than stacking exceptions.
2. **Durable provider and lifecycle.** Write exact DDL/legal-state table after cutover approval. Implement original acquisition, preparation, generation guards, publication, reading/resources, portable validation and removal/recovery. Extract the shared import scheduler/transfer seam with TXT behavior preserved. Verify at storage/reading boundaries before exposing unfinished imports.
3. **Product integration.** Browser and inbox EPUB routes, one Local Import queue/history/review flow, cover/detail/shelf capability wiring, semantic reader/TOC/note integration and existing preference behavior. Preserve TXT-specific controls only for TXT. Do not expose EPUB reparse controls.
4. **Composed verification and documentation.** Fresh isolated real-server upload and inbox journeys, read/link/note/resume/bookmark, backup/restore/removal, plus focused TXT/BookSource regressions. Update current usage/architecture docs when functionality actually lands. Complete the plan only at the recorded verification scope.

Each checkpoint is a cohesive tested commit or small sequence, not permission to expose a half-working EPUB workflow. Keep this plan's Current State/Next Action/Verification current at meaningful stopping points.

## Verification strategy

Use a few generated synthetic archives rather than a large copyrighted corpus. Fixtures cover format contracts, not special-case book names.

- Parser: EPUB 2/3 normal case plus one feature-dense semantic/navigation fixture; table-driven hostile archive/reference/limit cases exercising real boundary protections.
- Storage/lifecycle: exact reviewed publication, canceled/interrupted work, cleanup retry, cross-reader denial, portable round trip and quiescent restore. Reuse shared intake tests; add only EPUB-specific and mixed-format scheduling cases, not a copy of every TXT test.
- Reading/frontend: rich rendering and conversion preserve targets; main sequence vs TOC; note return and no auxiliary progress overwrite; image toggle performs no image requests; stale revisions reject reads/writes/resources; TXT/BookSource version-1 content remains readable.
- Race tests for changed scheduling/store/restore boundaries; focused normal tests first. Frontend typecheck/build and relevant component tests. One composed real-server journey for each intake channel, desktop/mobile reader inspection, no deployment/load claim.
- No universal EPUB conformance suite, benchmark project or large random corpus unless specific failures justify them. Optional legally supplied local EPUBs belong in repository-root `test-epubs/`, excluded from Git and Docker build contexts; they are compatibility evidence, never clean-checkout or CI dependencies. The user-supplied development example is `test-epubs/全职高手 (蝴蝶蓝).epub`; see Verification for the limited inspection performed. Real-book checks must be explicit local runs; default tests use synthetic archives. Do not commit extracted book content, images or generated reports containing substantial book text.

## Current State

`internal/fileimport` now schedules TXT and EPUB through one two-worker/two-transfer budget. Store-owned selectors provide immutable queue time and generation within one short read transaction; the service chooses the oldest request (TXT first on equal timestamps), then each store claims exactly that generation. Fair reader turns and one active preparation per reader remain unchanged. Startup and post-restore recovery cover both formats; shared cancellation/draining protects replacement, deletion and shutdown. No generic job framework, second pool or missing-table fallback was added. Existing TXT HTTP paths/codes remain unchanged; restore warnings now identify EPUB recovery failures as well.

Portable validation now includes private receipt/preparation ownership, section-index/physical stream checks, and resource registry/image-byte validation. Portable typed decoding now has shared byte/depth/token limits matching staged output. Local semantics, decoded summaries, cover identity and publication-wide section/resource/summary/target agreement pass isolated checks. The portable validators are assembled behind one storage-owned hook and registered with the production schema. Reader epoch **15** includes explicit publication binding and indexed section title/main membership in addition to EPUB storage and shared lifecycle integration. Verification uses fresh temporary homes only; epoch-13/14 homes/backups are rejected, not migrated, reset or modified. See Next Action and Verification for the remaining boundary and tested scope.

Publication and authorized reading/resource integration are implemented (details below). Storage-owned acceptance connects a ready generation to the library. EPUB browser acquisition/review HTTP is exposed through shared admission; inbox acquisition and the import/review UI remain pending (see the HTTP integration checkpoint below). The shared `internal/imageproc` validation boundary is implemented and used by EPUB: full static PNG/JPEG validation, caller-owned byte/dimension/pixel limits, context-aware reads, and orientation-aware display dimensions. Its result contains scalar metadata only; the module retains neither encoded inputs nor decoded images. EPUB retains admission budgets and translates shared errors into its existing failure categories, with a type alias rather than a duplicated image-info shape. Core boundary/orientation tests moved to the shared owner; EPUB keeps one translation/original-preservation regression and existing binding/cover/preparation coverage. The shared optimizer is now implemented: single validated decode, proportional no-upscale resize, EXIF orientation baked into capped 8-bit NRGBA pixels, bounded WebP output and actual encoder-backend identity. `gen2brain/webp v0.6.4`, indirect `purego v0.10.1`, and `x/image v0.42.0` are pinned; v0.42.0 avoids upgrading existing x/text/x/sys merely for resampling. Source metadata is stripped and standard Go decoder color interpretation is used, not ICC/gamma-managed conversion; Original remains the fidelity-preserving choice. The Dockerfile provisions native runtime libraries/loader links and runs the actual optimizer tests in the final image stage, without shipping the test executable or codec development packages. EPUB preparation now accepts copied `ImageOptions`: zero/original preserves the old path; optimized uses the fixed `webp-q92-edge2048-v1` profile and requires positive caller-owned per-derivative/aggregate byte budgets plus a synchronous output callback. It emits each referenced resource once, shares its returned ID and display metadata across cover/prose references, and caches no pixels or derivative bytes. Source validation is not repeated before optimization. `Preparation.ImageProcessing` records mode/profile, actual backend (empty if no conversion occurred), count and byte totals separately from content warnings. Sink failures, exhausted budgets and cancellation abort the entire attempt; the caller must discard all previously emitted sections/derivatives on any failure. All current callers are internal tests/proofs and now supply explicit options. Durable derivative storage, import toggle, fallback performance notice and custom-cover features are not implemented. The disposable local quality comparison was evaluated and removed as requested; none of its images, scripts or reports was committed.

- Dependency gate: isolated Readium v0.16.0 (`e36e46c88ad82c2ff9bfabc5657d988bd8c99cd4`) build succeeded on Go 1.27.0 linux/amd64. A caller doing only `epub.NewParser(nil)` had 790 compiled packages, 562 non-standard, 69 distinct modules including the proof module, and 310 cloud/AWS/gRPC/telemetry packages. Unstripped executable: 48,146,598 bytes. These are build-footprint observations, not runtime parsing benchmarks. No PDF-native dependency was observed in this import graph. The user explicitly selected focused Go implementation instead; no bounded Readium fetcher was built after this decision.
- Reproduction remains isolated from the application: create a temporary module, `go get github.com/readium/go-toolkit/pkg/parser/epub@v0.16.0`, compile `package main; import "github.com/readium/go-toolkit/pkg/parser/epub"; func main() { _ = epub.NewParser(nil) }`, then inspect `go list -deps -json .`. The local experiment and graph are in ignored `reference/epub-parser-proof/`; no upstream code or dependency was added to the application.
- `backend/internal/epub/` now owns bounded ZIP indexing/metadata reads, local URI resolution, bounded cancellable XML token decoding (including UTF-16 BOM and fixed HTML entities), and container/OPF inventory inspection. It preserves spine order, linear/non-linear membership, navigation IDs/properties and fallback IDs without treating TOC order as reading order. `Inspect` explicitly does **not** return a prepared/publishable book.
- Navigation inspection now reads EPUB2 spine-declared NCX `navMap/navPoint` or EPUB3 manifest-declared nav `epub:type="toc"` lists. It preserves document-order hierarchy, grouping headings, mixed inline labels and distinct path/fragment targets independently of spine. EPUB3 nav and legacy NCX are never merged; missing/invalid EPUB3 nav is diagnosed rather than silently substituted with NCX. XML namespaces are checked; base overrides are diagnosed as invalid optional navigation instead of ignored or fetched.
- Navigation uses the existing metadata/XML/context limits plus a 10,000-entry limit. Cancellation/limits and archive read failures remain fatal. Missing, malformed, empty or ambiguous optional navigation produces code-only diagnostics. Nonlocal, escaping, absent or nonmanifest targets retain label/hierarchy but no actionable target and are marked unavailable; missing labels have their own diagnostic. No target content or external DTD is fetched. A separate target-binding pass now checks fragments against registered normalized sections and emits section/opaque-anchor targets. Preparation orchestration now discovers and admits sections and supplies an explicitly labeled spine-derived list for empty navigation.
- Package support checks now reject declared fixed layout (EPUB 3 rendition vocabulary, including aliases and spine overrides; legacy fixed-layout metadata and Apple/Kobo display options). Encryption declarations permit only known IDPF/Adobe font obfuscation on declared font resources, which remain unused; encrypted non-font resources and other encryption are unsupported. No decryption or publisher-font rendering is implemented.
- Every spine item, including non-linear items, resolves through declared media fallbacks to an existing XHTML resource. Missing references/files, cycles and unsupported terminal media fail instead of silently dropping content. `SpineItem.ContentID` records the selected item while retaining original identity, order and linearity; fallback use has a code-only diagnostic. Multiple manifest IDs may alias one path when their media declarations agree; conflicting media declarations are rejected. The local example exposed why rejecting all aliases was unnecessarily strict; the fix applies to resource identity generally, not that book's IDs.
- `NormalizeSection` now converts one bounded XHTML document to a finite semantic tree: headings, paragraphs/groups, emphasis/strong/strike, sub/superscripts, code/preformatted text, lists, tables, ruby, line breaks, note roles and image/link bindings. It preserves language/direction and whitespace semantics. Block containers, inline groups, figures and figure captions now remain distinct so the renderer need not infer flow from surrounding nodes. The shared bounded XML tree now has its own owner (`xml_tree.go`) rather than duplicating navigation parsing. Styles/scripts and arbitrary publisher attributes are not emitted; unsupported meaningful content retains fallback text plus code-only diagnostics.
- Normalized anchors are opaque and separate from original IDs; ambiguous original IDs cannot resolve. Internal links and images use section-local opaque binding keys with private source-reference maps. HTTP/HTTPS external links are explicit-action candidates only; other schemes and remote images are inactive. A narrow SVG wrapper containing one image can yield a pending image binding; this is not SVG rendering or asset validation. Base overrides fail rather than silently redirect content. Empty readable content fails.
- This is an **internal preparation model**, not the frozen version-2 wire contract. It remains under `internal/epub` until the renderer proof establishes the final shared shape; existing version-1 endpoints are unchanged. No unvalidated binding is exposed for reading. Link/anchor binding over a completed section index, image-byte validation and per-section readability reassessment are implemented as described below. Staged preparation now connects these passes, discovers declared auxiliary documents and enforces admission before emitting bound sections.
- `backend/internal/reading/epub_document.go` now proves an explicit preparation-to-reading projection. The candidate version-2 shape retains the content envelope and `document.blocks`, with recursive prose nodes, leaf text, block/inline/figure semantics, and local targets containing `chapterIndex`, `contentRevision` and an optional opaque anchor. Qualified targets remain tied to their displayed revision even for delayed renderer events. Image references are issued by a caller-owned application callback using only section-local opaque keys; validated media type/dimensions are copied from binding evidence, never from archive paths or arbitrary publisher attributes. Unsupported source content retains safe fallback prose. Unknown node kinds, missing validated image bindings and cancellation fail rather than emit partial content. Mutable node/target/list containers do not alias canonical preparation state. Version-1 serialization, including empty block arrays, is unchanged. The registered EPUB reading provider now invokes this projection after storage authorizes the immutable ready publication. Reverting this unexposed increment needs no data conversion.
- Frontend candidate contract/renderer proof is implemented in `api/structured-prose.ts`, `features/reader/prose-nodes.ts` and the existing `ProseRenderer.vue`. The explicit parser admits finite nodes, bounded depth/node count, coherent revision-qualified targets, HTTP(S) external actions and same-origin application resource paths; it excludes publisher properties and rejects malformed leaf/action/resource shapes. A client-only `structureVersion` discriminator keeps legacy documents unambiguous. Legacy paragraphs/images adapt into the same renderer vocabulary; no second EPUB renderer or raw-HTML insertion was added. Structured documents retain authored headings and expose metadata title as an accessible article label, avoiding a duplicate generated heading. Application-owned elements preserve flow, lists/tables/ruby, language/direction and image dimensions; wide tables scroll locally. Failed images retain alt text; hiding structured images does not remove authored captions/other figure prose. Anchors are renderer-scoped data attributes, not publisher/global DOM IDs. Internal links require a caller-owned URL issuer and emit `{target, note}` with the original revision and explicit `noteref` intent, including notes within main sections; without an issuer they are non-actionable. External links require user activation and isolate the opener/referrer. Display conversion visits nested text/alt values without changing canonical trees, target identifiers or URLs. **`getChapterContent` now admits versions 1 and 2 through their separate strict parsers**, following the shared Go wire-fixture and composed ReaderView browser proof. The shared session now accepts both typed document shapes internally, with candidate version-2 loader/conversion reuse and note/anchor lifecycle integration tested below. Backend EPUB responses are now served through the registered provider; acquisition/review UI remains pending.
- Optional catalog `auxiliary` membership is implemented across shared reading types and frontend parsing. The registered `epubCatalog` projection preserves prepared section ordinals and emits the flag only for auxiliary sections; legacy wire shape is unchanged. The projection includes a separate optional `navigation` tree with provider-neutral provenance (`publication` for authored EPUB 2/3 TOCs, `sections` for preparation's explicit spine fallback). Entries retain labels, grouping-only nodes, unavailable targets and children, and use the same revision-qualified ReadingTarget as prose links. TOC ordering/repeated section targets do not change the section inventory or main sequence. Legacy providers omit navigation entirely. `api/catalog-navigation.ts` validates finite bounded trees and target membership/revisions; `getCatalog`/`waitForCatalog` retain this evidence. Reader and Book Detail now consume it through the user-selected always-expanded outline (`TocNavigationList.vue`), preserving the incumbent visual style, semantic nested lists, inactive group/unavailable labels, ancestor-preserving search, current-section focus and authored order (no reverse control). Pure TOC helpers own search/highlighting/text mapping; the list only emits existing ReadingTargets or builds qualified reader links. ReaderView stores canonical/display catalog navigation separately from transient visit state and routes selections through its existing anchor/note lifecycle; conversion caches labels by catalog/mode without changing targets or canonical text. Legacy catalogs retain their flat chapter list and controls. Shared location validation permits auxiliary bookmark locations but rejects them for progress before the library write. Frontend ordinary previous/next, prefetch and saved-resume fallback use main membership; explicit auxiliary locations remain reachable. ReaderView neither mutates its local main resume nor queues progress when auxiliary content is displayed. Existing providers still emit main sections only; there is no live EPUB adapter. Untitled-section bookmarks now use successful revision-qualified location lookup rather than title presence as identity: the shared service preserves empty title metadata, while the bookmark sheet displays the existing one-based section number convention. No synthesized title is persisted.
- `features/reader/reader-navigation.ts` proves revision-bound immutable transition proposals: nested note returns restore one captured position at a time, including same-main-section notes; note visits disable ordinary progression/prefetch and main writes. Explicit main links/chapter navigation leave note context. Departure captures actual normalized position, not an obsolete entry anchor. Failed/unapplied proposals cannot consume the old trail. This model is now wired into ReaderView: document/index/navigation commit together under its request generation; scoped anchor restoration awaits layout before progress can be queued, and a missing anchor rolls back the previous document, trail and position. Nested Return, refresh and direct `note=1` visits remain write-free, including same-main-section notes. The existing footer exposes Return one step at a time; main counters exclude auxiliary sections and note visits display a localized label. Note detours leave the origin route intact; issued anchor links carry revision and explicit note intent for new tabs/reloads. Ordinary main navigation clears note context and updates the qualified route. Bookmarks/progress cannot capture an in-progress scroll restoration. Renderer link clicks and locally scrolling tables retain their interactions rather than triggering reader keys/taps. Per-book state remains transient, not cross-book authorization or persisted history. Earlier integration tests inject structured documents at the loader seam; the composed browser proof now exercises actual version-2 transport parsing with mocked API responses.
- `targets.go` / `target_bindings.go` register copied path/anchor metadata without retaining section trees, then resolve forward/back/self links and hierarchical navigation over the completed index. Targets contain only an interpretation-local ordinal and opaque anchor; missing/ambiguous fragments never fall back to document start. Broken links retain text and an unavailable marker; external HTTP(S) actions remain untouched. Multiple spine occurrences of one resource keep self-links local, while ambiguous cross-document/TOC targets remain unavailable rather than selecting an arbitrary occurrence. Original IDs/paths do not enter resolved trees/navigation. Registration limits are 15,000 sections (spine plus manifest bounds), 250,000 anchor entries and 32 MiB of copied path/anchor text, plus bounded map overhead; cancellation/limit failure leaves registration unchanged. These are internal proof limits, not a frozen wire or persistence contract. The index does not discover/admit sections, infer fragment equivalence across fallback resources, choose main reading order or qualify targets with revisions; those remain orchestrator/reading-boundary responsibilities.
- Static image validation now uses Go's JPEG/PNG decoders: declared format must decode, dimensions are checked before bitmap allocation, and full pixel decoding detects corrupt payloads that header inspection would accept. Initial limits are 16 MiB encoded bytes, 16,384 per dimension and 32 Mi pixels. APNG is rejected because the standard decoder validates only its default image; GIF/WebP/SVG and other formats are explicit unsupported-image diagnostics, not presumed safe. Original-mode preparation remains unchanged despite the separate optimizer implementation. Its orientation-aware display dimensions retain unchanged source bytes. Encoded matrix dimensions still enforce decoding budgets; `imageproc/orientation.go` reads only the primary EXIF orientation (JPEG APP1 and PNG eXIf), swapping display axes for orientations 5–8. Missing/malformed metadata has no orientation; no thumbnail-directory traversal or other EXIF data is retained. The actual renderer reserves stable before/after-decode geometry with those dimensions in the recorded desktop/mobile Chromium check.
- `Prepare` now owns the bounded parser orchestration: one archive index and actual-read budget, caller-owned empty disposable seekable scratch, then one bound section per callback after admission/target discovery. It seeds every spine slot first (including non-linear slots), discovers only existing declared XHTML from TOC/internal links, and terminates note cycles by manifest path. No remote fetch or arbitrary manifest sweep occurs. Main membership is explicit and is not inferred from an ordinal prefix. Missing navigation yields `Source=spine` with main-only entries; unavailable individual targets retain their diagnostics. Private validated image references/media evidence stay separate from presentation trees. Caller owns scratch cleanup and must discard the entire output attempt on any preparation, cancellation or callback error; no partial `Preparation` result is returned. This is not an alternative durable store or a publication transaction.
- Embedded cover selection is implemented in preparation. EPUB 3 `cover-image` declarations take precedence over legacy OPF `<meta name="cover" content="…">`; same-resource aliases are one choice, but conflicting declarations (including a known plus unresolved ID) yield `cover_image_ambiguous` and no selection. If a non-conflicting declaration is missing/unusable, an unambiguous validated image from an explicitly identified cover page may be used, retaining degradation diagnostics. Page evidence is captured before image validation removes bad bindings, so a surviving decorative logo cannot become a guessed cover. Repeated references to one resource are not competing artwork. Cover-only pages outside the reading graph are inspected for artwork without adding sections or following their links. No filename, ordinary-chapter or dimension heuristics; no whole-manifest content sweep. Absent artwork is represented by `Preparation.Cover == nil` for the existing default-art fallback. `Cover` contains private validated `PreparedImage` evidence, not a public URL, copied asset or new storage/customization owner. Fatal read/limit/cancellation errors still abort preparation before emission.
- Accepted cover admission is implemented: an explicitly identified cover that becomes unreadable retains its section position with `CoverPlaceholder` and review diagnostics only if some main content remains readable. Recognition uses body-level EPUB cover semantics, whole-document OPF guide references or EPUB 3 cover landmarks—not filenames or image-only heuristics. A fragment landmark cannot exempt an entire mixed-content document. Other unreadable/malformed spine sections fail, including explicit non-linear spine entries; malformed/unsupported/unreadable optional referenced XHTML remains unavailable with diagnostics. Cancellation, limits, ZIP/read corruption and scratch/output failures are never softened into optional warnings.
- Staging budgets: 64 MiB JSON per section, 2 GiB total scratch and 4 MiB aggregate section-title text, in addition to existing input/index/image bounds. One normalized tree is serialized/read at a time; anchors live in the bounded target index rather than being copied into scratch. JSON serialization still allocates a per-section buffer; these are not zero-copy/constant-memory claims. OCF names and decoded URI fragments now reject invalid UTF-8 at the input boundary so JSON cannot replace bytes and silently retarget a link.
- A preparation-local image resolver checks only declared, referenced archive resources. It retains bounded validation metadata by manifest path, not decoded pixels/bytes, so repeated illustrations are validated once. Valid image nodes receive dimensions; missing/invalid/unsupported images become non-actionable placeholders with alt text and code-only diagnostics. Cancellation, ZIP/read corruption and size-limit errors remain fatal. Readability is recomputed after binding; it is evidence for whole-book admission, not an automatic failure for a missing cover or permission to publish an empty book. Preparation emits validated image metadata/private references; `epubstore` persists optimized derivatives while Original mode retains archive-backed images. Authorized image delivery is implemented in the publication/reading integration below.
- Section input is capped at 16 MiB and shares the XML depth/token bounds. Archive reads enforce actual bytes cumulatively (up to 1 GiB for content), rather than trusting declared sizes. No section segmentation/virtualization was added. Ordered-list starts retain signed 32-bit semantics; table spans outside the initial 1–1000 range are diagnosed and omitted. These preparation limits/profile still need the renderer/cache-size proof before freezing.
- Initial inspection limits: 256 MiB compressed archive, 20,000 ZIP entries, 1 GiB declared expanded size, 4 MiB per metadata read, 16 MiB cumulative metadata read budget, XML depth 128 / 200,000 tokens, 10,000 manifest items and 5,000 spine items. These are internal limits, not settings. Standalone inspection does not decompress chapters/images; `Prepare` reuses that same archive and includes metadata, section and image reads in the cumulative content budget.
- The optional local example passes inventory/navigation inspection (16,778,888 compressed bytes, 1,760 manifest items, 1,737 spine entries, 1,736 navigation entries, no navigation or support diagnostics). The explicit normalization/image check processed all 1,737 spine sections, one at a time: 18,359,861 XHTML input bytes, 25,582,370 semantic-tree JSON bytes, 3,472 valid image bindings from 16 distinct resource checks, zero unreadable sections/local link bindings/anchors, and no diagnostics in 2.27 seconds locally. Target binding also resolves all 1,736 navigation entries, with none unavailable. This is elapsed compatibility-test time, not a throughput benchmark or performance comparison. Unreferenced assets were not decoded. This book does not exercise internal-link/anchor compatibility; synthetic tests do. Target fragments are tested synthetically, not by this local book. The subsequent full staged preparation proof also passes for this example (measurements below). Embedded cover selection now also passes the staged local-book proof. Authorized resource serving and actual local-book browser reading remain unvalidated; the synthetic shared-renderer browser proof below is not full-book compatibility.
- Image optimization adds only the pinned dependencies listed above. The shared-worker/storage checkpoint activates epoch 14 without changing existing homes/backups. Frontend import/review exposure remains pending; shared reader/removal integration is implemented. Untracked frontend prototypes remain untouched.

- Cover placeholders now survive prepared-section emission and version-2 projection as an optional `document.coverPlaceholder` flag. The user selected existing default artwork with a localized “Original cover unavailable” caption. ProseRenderer reuses BookCover and retains the semantic tree/anchors; the substitute and caption are application presentation, never injected book text or a resource binding. Disabling images hides the artwork but retains the caption. The flag is absent for ordinary/legacy documents; admission still requires an explicitly identified cover and other readable main content.

- Auxiliary bookmark reopen proof now passes without production changes: the bookmark sheet emits its stored revision/index/position; a fresh qualified URL opens the auxiliary section at that position with no fabricated return trail. Refresh restores actual scroll position, capture retains the auxiliary location, and prefetch, progress persistence and unmount do not issue auxiliary progress writes or replace the local main resume. This is mocked ReaderView integration plus existing backend location/library tests, not yet a persisted EPUB end-to-end journey.

## Storage checkpoint — persistence, portable validation and shared lifecycle integrated

This checkpoint is High-Risk because it changes reader-home and portable-data contracts. The authorized epoch 14 is implemented with EPUB persistence, portable hooks and shared worker lifecycle registered together. Existing TXT behavior is retained; EPUB publication/resource HTTP is implemented in the epoch-15 integration below.

- `epubstore` owns receipts, immutable preparation attempts, sections/resource mappings, original/derivative files and their recovery. Library metadata/progress/bookmarks remain library-owned. Retain EPUB-specific tables rather than forcing its preparation model into TXT interpretations.
- Durable intent precedes moving files into managed storage. Expensive normalization/encoding happens outside the reader mutation gate in an attempt-owned disposable directory. Finalization takes the existing `FileStore.LockMutation` gate, checks the current attempt, installs its immutable files and commits the matching database references. Interrupted finalization must be recoverable; no ready/publication state may point to incomplete output.
- Receipt implementation: `epub_files` persists acquisition separately from the preparation-attempt rows, through a borrowed-home `Store`. The EPUB schema is composed through `epubstore.ReaderSchema()`. Explicit `receiving → finalizing → acquired` states distinguish incomplete streams from durable move intent; `failed` retains evidence, and `removing` retains cleanup ownership. Originals use `epub/<server-ID>/original.epub`; filenames are display metadata, not path components. The requested image mode is snapshotted at receipt creation and will seed the first preparation generation. `Receive` persists complete-transfer intent before moving the synced original; `RecoverAcquisitions` reconciles both sides of that move and interrupted removals with intake quiescent under the existing mutation gate. Incomplete or size-mismatched transfers become failed receipts rather than acquired files; failed cleanup retains ownership for retry. Discard requires callers to cancel/join receipt work first. TXT and EPUB reuse the small confined, context-aware `readerstore.WriteWorkFile` boundary; format-specific policy and state machines remain separate. Tests use fresh synthetic homes; existing data is not converted.
- Preparation ownership is implemented in the registered schema: a monotonic current-generation counter on each acquired receipt and separate attempt rows. Queue/retry snapshots the receipt image mode transactionally; replacing an active attempt marks it superseded without giving its late worker permission to update current state. Claim/failure writes require both the generation and expected state. Generation finalization now persists ready-state output and has explicit startup reconciliation. Shared dispatch/recovery is application-wired; no separate framework was introduced.
- Prepared-output persistence is implemented: `Store.Prepare` owns claim, original access, staging, generation-guarded index/summary intent, and installation into `epub/<receipt>/preparations/<generation>` under the mutation gate. Section spans, one resource row per validated source image, preparation format version, summary/diagnostics, and image profile/backend evidence are committed with finalizing intent before directory movement. Only a matching current generation can become ready. Ready means complete prepared output, not review approval/publication. Retries cannot supersede finalizing/ready output; failed finalization must be reconciled first. `RecoverPreparations`, called after acquisition recovery with intake/workers quiescent, resumes finalizing directory moves and checks contiguous stream coverage, decoded record ordinals, image-binding registry consistency, and derivative file sizes before readiness. Known incomplete output is removed and failed; cancellation, database errors and other unknown/transient errors leave recoverable output intact and are returned. Interrupted running work is failed for explicit retry; successful recovery removes only the private disposable EPUB work subtree. Portable validation and lifecycle recovery are now registered.
- Persist acquisition state separately from preparation state. A retry gets a new generation and preserves the selected image mode; stale completion cannot replace newer work. No published reparse/history UI or generic job framework.
- Preparation evidence includes parser/profile version, requested image mode, actual encoder backend, diagnostics, catalog/navigation and cover/resource identity. A resource identifies either a validated original ZIP member or a generated derivative; archive names never become filesystem destinations or public URLs. Reading loads one stored semantic section and delivers one authorized resource, never reruns preparation.
- Original-resource storage is now explicitly selected: retain original-mode images only in the unchanged EPUB and read the requested ZIP member for delivery. Do not materialize a second original-image copy. This saves disk/backup space at the cost of ZIP-index reading and member decompression on uncached requests; pixel validation remains import-time work. Optimized derivatives are separate immutable files. `epub.ReadOriginalImage` supplies the bounded private read boundary, not HTTP authorization or portable validation. Storage must resolve authorized opaque IDs to validated references, never forward client archive paths. The registered schema owns section spans and resource identity; public resource authorization remains separate.
- `epubstore.Stage` now owns a randomly named disposable attempt inside a caller-provided private work root (`os.Root`). Sections are serialized one at a time into a single uncompressed `sections.jsonl` stream with contiguous byte offset/length spans; the stream is synced once after preparation rather than once per section. Derivative IDs are random and shared across cover/prose through preparation's metadata-only cache. Original image bytes are not copied. Final section output is bounded to 64 MiB each / 2 GiB aggregate; derivatives to 16 MiB each / 1 GiB aggregate, independent of compressed source size. Files are synced/closed, scratch is removed before return, and any error discards the entire attempt while retaining cleanup errors. Success transfers cleanup ownership through `StagedPreparation.Close`; the work root must outlive it. `Stage` alone still conveys no publication or database state; those responsibilities now live in `Store.Prepare` and recovery. `PreparedSection` reads a single current-ready range without loading the catalog, and `PreparedMetadata` returns private preparation evidence. Neither method performs publication/revision authorization. No section-tree or bitmap cache was added.
- Verification must cover fresh acquisition/preparation, stale-attempt rejection, failure cleanup/recovery, confinement, old-epoch rejection without modification, and portable consistency. Add publication/resource-route/worker/UI tests as those boundaries land, not prematurely.
- Rollback: use the previous application revision with its preserved epoch-13 home/archive. Do not open epoch-14 data with the old binary, migrate backward, overwrite old data, or silently reset mismatched homes. Integration checks use a separate fresh root; no deployment or live-data reset is authorized.

## Storage-layout verification — indexed stream accepted

Code inspection and a bounded synthetic comparison revise the prior **one-file-per-section** recommendation. Recommend **one immutable uncompressed JSON section stream per generation, with SQLite ordinal/offset/length indexes**, while keeping originals and optional image derivatives as already selected. The user has accepted this representation. Staging now emits one bounded stream and byte spans; a private bounded ReaderAt-based reader retrieves individual records without shared seek state or a whole-book cache. SQLite span/resource persistence and generation finalization are implemented and registered at epoch 14. Existing-data conversion remains out of scope.

- Existing TXT stores one original plus SQLite byte-range indexes (`txtstore/catalog.go`, `txtstore/analyze.go`), not a file per chapter. EPUB's `preparation_stage.go` already demonstrates bounded JSON records plus offset/length spans. Use ordinary bounded range reads, not a custom archive engine, whole-stream decompression, runtime storage selector or new cache. Keep offsets/layout hidden inside `epubstore`.
- `readerstore/backup.go` holds the mutation gate across `VACUUM INTO` and durable-file copying, then releases it before private-copy validation while retaining the home lease. `.work` is omitted. Restore copies the extracted payload into replacement staging, validates it, and replaces a drained home with rollback support. The existing schema callbacks can own EPUB validation without teaching backup code EPUB semantics; those callbacks and shared worker quiescence are implemented.
- Backup export and restore now share the existing 100,000 logical-entry, 2 GiB compressed and 8 GiB entry-payload limits, plus the existing manifest-size bound. At 1,737 sections/book, 58 books already yield 100,746 section files before directories, originals or derivatives. This is arithmetic illustrating a real format limit, not a library-scale benchmark. Packing sections avoids the chapter-count multiplier; image counts and byte limits still apply. Export now checks all entry payloads and compressed output through compressor finalization; failures return errors rather than successful over-limit downloads. Shared subtraction-based payload accounting also prevents overflow on untrusted restore sizes. Synthetic exact-limit roundtrips and over-limit rejection pass, including directories and long-path PAX metadata. No limits were increased, no runtime tuning was added, and no schema or existing home/archive was changed. EPUB-specific portable validation is now registered.
- Copying is a full snapshot, not incremental or deduplicated. Neither layout removes temporary snapshot/restore space: restore temporarily holds extracted and replacement copies alongside the existing home. Packing does not compress prose on disk or eliminate retained originals/derivatives. Superseded managed output needs explicit owned cleanup; `copyDurableFiles` copies all durable files, not only active DB references.

Synthetic evaluation: Go 1.27 linux/amd64, 1,000 JSON records totaling 16,409,000 bytes, three sequential repetitions/layout. Used the actual reader Manager/schema, WAL settings, SnapshotHome, PrepareReplacement and PublishReplacement; restored first/middle/last records matched. External records were synced individually versus once for the stream; SQLite bodies used one prepared-statement transaction. All layouts indexed section identity in SQLite. Disk-backed Linux filesystem (`stat -f`: ext2/ext3), median timings:

| Layout | Content preparation | DB/index commit | Snapshot | Replacement | Warm random read | Snapshot regular files |
|---|---:|---:|---:|---:|---:|---:|
| Per-section files | 1,560 ms | 4.85 ms | 137.78 ms | 153.73 ms | 53.47 µs | 1,003 |
| Indexed stream | 31.05 ms | 4.69 ms | 33.23 ms | 44.88 ms | 46.43 µs | 4 |
| SQLite bodies | included in commit | 194.80 ms | 136.82 ms | 49.62 ms | 89.20 µs | 3 |

Logical snapshot sizes: files/stream 16,437,730 bytes; SQLite 16,908,346 bytes. Allocations per warm read: about 19.5/19.4/37.4 KB respectively; **not peak RSS or retained heap**. Initial tmpfs runs were separated from these disk-backed figures. This comparison omits EPUB normalization, images, feature-specific portable validation, compression/archive transfer and final generation-directory installation. It does not prove cold-read latency, concurrent progress responsiveness, physical allocated bytes, power-loss durability, leak freedom or performance on other deployments. No universal speedup claim is warranted. Probe/results are local ignored `reference/epub-storage-evaluation/` artifacts; only synthetic data was used, with fresh disposable homes and no production changes.

Portable roundtrip, corrupt/missing reference rejection, generation recovery and shared worker-draining verification are recorded below. The layout benchmark is historical evidence, not a concurrency or full-book throughput claim.

## Publication/reading integration — implemented

The user approved a fresh-data epoch-15 cutover after checking the direction against existing TXT ownership. Implemented an explicit nullable `epub_files.library_id` (unique, same-ID library foreign key), not an inferred publication or a generic binding framework. Acceptance atomically links one current ready preparation to a library item; retries return that publication. Library metadata, content revision and state version remain library-owned and independent of preparation generations. Ready EPUB preparation is immutable in this milestone; published re-preparation remains out of scope.

Protect published receipts from draft discard; publication removal first commits library removal plus durable file-removal intent, then performs retryable provider-owned cleanup. Portable validation checks the link in both directions. Reading, covers and images require the authenticated reader's publication and matching content revision; paths and preparation evidence never become resource URLs. Use existing catalog/content projection and shared reader contracts. Section location/progress queries must use indexed section metadata rather than decode whole-book preparation metadata or section trees. No new cache, worker pool or conversion path.

Epoch 15 now includes the complete publication schema: nullable publication link and `epub_sections.title/main`, validated against staged metadata and portable stream evidence. Existing epoch-13/14 homes and backups remain untouched: no migration, reset or deployment. Rollback uses the previous binary with its corresponding preserved home. Verify with minimal synthetic books and focused ownership/revision, reading/resource and removal/restore boundary tests; no corpus or novel images may be committed.

Implemented contracts:
- `epubstore.Accept` guards the exact ready generation, atomically inserts a `library.EPUB` item at content revision 1 and links the receipt; repeat acceptance returns the existing item. The library owns display metadata/revisions; generation is never aliased to revision.
- `GetCatalog`, `GetSection`, `ReadSection` and `ReadResource` require current publication identity. Indexed section location/content reads do not decode whole-book metadata. Single-section image keys map to durable opaque resource IDs before URL issuance.
- Registered `reading.Service` EPUB provider uses `epubCatalog`/`epubContent`, existing auxiliary-location rules and shared progress/bookmarks. Resource/cover URLs use `/api/books/{id}/epub-resources/{resource}?revision=…&reader=…`; authenticated outer routes retain home leases. Stored media type, nosniff and private no-store apply. Original delivery reads the validated ZIP member; optimized delivery reads one bounded derivative. Neither re-prepares nor validates pixels.
- `RemovePublication` commits library deletion with removing intent before cleanup; failures return `epub_cleanup_pending`, retryable by ID or recovery. Draft discard rejects linked receipts. Portable checks prove both link directions, current-main-section/title/count/revision/cover consistency, with prior stream/semantic/resource checks unchanged.
- Shared library DTOs expose `provider: epub`, `originLabel: EPUB`, and authorized coverDisplayUrl (not the internal opaque cover ID). Existing frontend parsers/renderer already admit the shared contract. Book Detail adds EPUB destructive-removal copy and cleanup warning/retry handling; TXT reparse remains TXT-only.

## Import HTTP/UI integration — browser HTTP implemented, UI pending

Use format-neutral `/api/imports/admission` endpoints backed by the existing shared admission owner. Keep the existing TXT admission routes compatible, with shared implementation rather than a second scheduler. Upload, receipt/review and format options remain TXT/EPUB-specific. The frontend will reuse one queue with typed format-specific operations, not a plugin registry or generic job framework.

The current slice connects EPUB browser acquisition, lightweight receipt history/status, exact-generation review/retry/acceptance and safe draft discard through authenticated HTTP. Preparation remains worker-owned; receipt polling must not decode preparation metadata. Review projects saved metadata, a bounded contents page and a bounded plain-text sample, never archive paths or private binding maps. Encoder performance notices are separate from content diagnostics. Retry is only for an unprepared/failed current generation; discard rejects running/finalizing preparation rather than introducing a reader-wide pause from an ordinary request. No schema/data conversion or UI exposure in this slice. Fresh synthetic HTTP upload → shared worker → paged preview → publication, reader isolation, TXT admission compatibility, failed-generation retry/discard and UTF-8 sample bounds now pass targeted tests. Responses omit archive paths/bindings and raw storage errors; new preparation failures persist stable error categories. Review samples are capped at 4096 bytes and contents pages at 100 entries. Affected-area race suites, focused portable-encoder HTTP checks and final diff review passed (commands below). EPUB inbox acquisition and UI remain pending.

## Next Action

Connect the shared frontend queue, EPUB review UI and inbox acquisition using the completed storage-owned acceptance and shared import service. Keep storage paths and private preparation maps behind `epubstore`; reading must not rerun preparation or image validation. Publication ownership/cleanup is now integrated with the library owner; continue to avoid a temporary reading bypass.

Include explicit portable-encoder notices in that review flow, then verify the complete import → read → backup/restore → removal journey. Shared EPUB reading/resource routes are exposed, but acquisition/review UI remains pending. Existing reader/browser proofs use mocked APIs, not real EPUB publication. No deployment or live-data reset is authorized.

## Verification

Recorded checks (backend commands run from `backend/`, frontend commands from `frontend/`):

- Browser import HTTP: `GOPATH=/tmp/novelreader-webp-go go test -race ./internal/api ./internal/epubstore ./internal/fileimport -count=1` passed. After final tightening, focused `go test -race ./internal/api ./internal/epubstore -run 'Test(EPUB|ImportControls|ReviewSample)' -count=1` passed. `CGO_ENABLED=0 GOPATH=/tmp/novelreader-webp-go go test -tags nodynamic ./internal/api -run 'TestEPUB(BrowserImport|FailedImport|PortableEncoderNotice)' -count=1` passed, including a real optimized import and persisted portable-backend notice. Synthetic tests cover the shared/legacy admission contract, unread invalid-policy bodies, acquisition versus preparation, optimized upload → worker → bounded review → idempotent publication, reader isolation, stale-generation denial, failed retry/discard, active-preparation exclusion and UTF-8 sample limits. Existing API race coverage also exercises blocked upload cancellation, TXT lifecycle and restore/shutdown. `git diff --check` passed. AFT inspections timed out; compiled tests are authoritative. No frontend/browser, full repository suite, deployment or existing-data verification was performed for this slice.

- Publication/epoch-15 integration: `GOPATH=/tmp/novelreader-webp-go go test ./internal/epubstore ./internal/reading ./internal/api` and `GOPATH=/tmp/novelreader-webp-go go test -race ./internal/fileimport ./internal/epubstore ./internal/reading ./internal/api ./internal/readerstore ./internal/backup ./cmd/server` passed. Synthetic storage tests cover exact generation, idempotence, publication rollback, both image modes, metadata-independent indexed reads, portable publication roundtrip and wrong binding/index rejection, durable removal failure/recovery. Real authenticated HTTP tests cover catalog/navigation/note+auxiliary projection, images/covers, main progress and auxiliary bookmarks, wrong-reader/unpublished/stale/removed denial. This is not browser proof. After tightening current-title validation and adding acceptance-rollback coverage, `go test -race ./internal/epubstore -run 'Test(Publication|Portable)'` passed again.
- Frontend: `npm test -- --reporter=dot src/features/books/BookDetailView.test.ts src/features/reader/ReaderView.navigation.test.ts src/api/structured-prose.test.ts` passed (27 tests); `npm run typecheck` passed. No real-browser journey or full suite was run for this integration. Scoped ESLint for the changed Book Detail component/test and three locale files passed with no warnings. Independent review reran the publication, authenticated-resource, removal and portable-ownership boundary tests under race detection; they passed. One narrow AFT inspection timed out; compiled tests are authoritative.

- Shared-worker/epoch-14 checkpoint: `go test -race ./internal/fileimport ./internal/txtstore ./internal/epubstore ./internal/readerstore ./internal/backup ./internal/api ./cmd/server -count=1` passed. After tightening selection to a coherent read snapshot and removing the superseded TXT worker entry point, affected store/worker race suites passed again; server schema checks include all four EPUB tables. `git diff --check` passes. AFT diagnostics timed out; compiled tests are authoritative.
- New focused integration cases prove both cross-format age orderings, exact-generation claims after replacement, progression past a failed EPUB, startup recovery of interrupted EPUB alongside TXT work, and cancel/drain → snapshot → replacement → recovery → resume through the real shared dispatcher. Existing pool/admission/API tests cover the retained budgets, fair turns and shutdown/deletion barriers. Epoch mismatch tests now reject epoch 13 without changing its schema marker. All homes are fresh temporary fixtures; no deployment, existing data, real-book browser journey or concurrency throughput benchmark was exercised.

- Import-owner promotion: the pre-move `txtimport` suite passed. After moving to `fileimport`, race tests passed for its admission/pool/startup/capacity cases and affected API TXT/restore/server-close cases (`go test -race ./internal/fileimport ./internal/api -run 'Test(Admission|Pool|Start|Capacity|TXT|Restore|ServerClose)' -count=1`). Existing tests moved rather than being duplicated. AFT diagnostics were unavailable (transport timeout); compiled tests are the verification gate. This does not establish EPUB worker behavior or mixed-format throughput.

- Composed portable hook: targeted snapshot/replacement tests and the full `epubstore` race suite pass. The negative replacement check preserves an existing target receipt and the original source preparation. `git diff --check` passes; AFT timed out. The live reader schema remains unchanged; worker shutdown/quiescence and application registration are not yet verified.

- Publication-wide evidence: normal and race tests for `epub` and `epubstore` pass. Coverage includes forward auxiliary links/backlinks, section-qualified duplicate anchor names, missing section/TOC/link targets, summary disagreement, and registry mismatch. Existing synthetic storage/portable roundtrips now include real parsed forward/back links. Same-size anchor corruption and registry mismatches reject both ready and finalizing output. `git diff --check` passes; AFT timed out. No live schema, worker-quiescence, or publication-authorization claim is made.

- Section-local semantics: normal and race tests for `epub` and `epubstore` pass, including rich normalized XHTML, image/link/anchor/readability rejection, valid unavailable covers, depth/cancellation and same-size semantic corruption of ready/finalizing streams. Existing synthetic preparation and portable roundtrips still pass. `git diff --check` passes; AFT timed out. Publication-wide binding/anchor proof, live registration and end-to-end publication remain unverified.

- JSON allocation boundary: full normal and race tests for `epubstore` pass, including existing synthetic preparation/portable roundtrips. Focused regressions exercise depth/token boundaries, escaped delimiters, scalar/null-array expansion, cancellation, write/restore rejection, and oversized/deep SQL metadata. `git diff --check` passes. AFT timed out; compiled tests are authoritative. These checks establish finite structural bounds, not measured worst-case RSS or semantic validity.

- Decoded-summary validation: normal and race tests for `epub` and `epubstore` pass; focused CGO-disabled `nodynamic` summary/resource/portable-roundtrip tests pass, as does `git diff --check`. Tests cover legitimate auxiliary/group/unavailable navigation, image-free optimization, backend evidence, navigation bounds/cancellation, and mismatched persisted section counts, derivative totals and cover identity. AFT lacks authoritative Go diagnostics (`gopls` unavailable). No JSON-allocation, semantic-tree, live-schema or end-to-end publication claim is made.

- Resource validation: normal package tests for `imageproc`, `epub`, and `epubstore`, race tests for those packages plus `backup`, and focused CGO-disabled `nodynamic` optimizer/resource/snapshot tests pass. Synthetic coverage includes both image modes, false dimensions, same-size damaged bytes, metadata-bearing WebP, pixel limits, cancellation and interrupted derivative recovery. The existing portable roundtrip now covers optimized output. AFT timed out; `git diff --check` passes. No full semantic-validation or live-registration claim is made.

- Snapshot lock scope: the new concurrent-mutation/copy-independence regression fails against the previous `backup.go` through a temporary Go overlay and passes with the change. Race tests for `readerstore`, `backup`, `txtstore`, and `epubstore`, plus `git diff --check`, pass. AFT had no authoritative Go diagnostics because `gopls` was unavailable; compiled tests are the verification gate.

- Physical streams: targeted `TestPortable|TestIncompletePreparation` tests and race tests for `epubstore`, `readerstore`, and `backup` pass. Synthetic missing/truncated/invalid-JSON/wrong-ordinal streams reject ready output but preserve interrupted finalizations for failed/retry recovery. Snapshot/replacement also covers damaged installed finalization; mixed unknown/cancellation errors do not authorize incomplete-output cleanup. AFT inspection timed out. Full untrusted semantic/resource validation and live registration remain pending.

- Portable section indexes: targeted `TestPortable|TestSectionStream` tests and race tests for `epubstore`, `readerstore`, and `backup` pass. Synthetic checks cover ordinal gaps, overlaps, missing tail coverage, oversized spans, invalid format/state, orphan rows, and retained failed-output evidence. Existing snapshot/replacement cases now compose ownership and index checks. AFT inspection timed out; no full portable semantic validation or live schema activation is claimed.

- Portable ownership validation: targeted `TestPortable` tests and race tests for `epubstore`, `readerstore`, and `backup` pass. AFT inspection timed out. Prepared section/resource/semantic validation is not implemented by this component; complete untrusted EPUB restore safety remains unverified.

- Portable preparation ownership: targeted snapshot/recovery tests and race tests for `epubstore`, `readerstore`, and `backup` pass. Cleanup and ownership checks are registered only in isolated test schemas; this is not proof of safe untrusted EPUB restoration or production registration. AFT inspection timed out.

- Archive-limit compatibility: backup package tests and race tests for `backup`/`readerstore` pass. Synthetic tests cover an exact-limit export/extraction roundtrip, each over-limit dimension, directory/PAX counting, payload-size overflow, manifest size, and output failures. Existing cross-reader replacement tests pass. AFT inspection timed out; Go tests are authoritative. This is archive-boundary verification, not EPUB semantic validation, an EPUB backup/restore roundtrip, or a multi-gigabyte stress test.

- Durable preparation: normal `epubstore` tests; race tests for `epubstore`, `epub`, `reading`, `readerstore`, `backup`; and CGO-disabled `nodynamic` `epubstore` tests pass. Fresh synthetic homes cover both image modes through acquisition → queue → prepare → indexed read → discard, resource/cover identity and encoder evidence, stale-finalizer rejection, atomic rollback on resource-index insertion failure, persisted intent around directory moves/readiness-write failure, incomplete derivative cleanup, interrupted-worker retry, repeated recovery, receipt-scoped work removal, and preserving output after a database lookup failure. Existing backup tests pass, but no EPUB portable roundtrip, HTTP authorization, power-loss durability, physical memory/performance benchmark or deployment is claimed. AFT inspection timed out. The live reader schema remains epoch 13.

- Indexed-stream staging: normal `epubstore` tests, race tests for `epubstore`, `epub`, `reading`, and CGO-disabled `nodynamic` `epubstore` tests pass. Synthetic cases verify contiguous full-file coverage, Unicode/random section reads confined to their spans, invalid/oversized spans, ordinal mismatch, multiple records in one span, truncated input, cancellation, aggregate output limits and short writes. Existing original/optimized image identity and cleanup tests pass with exactly one section stream. AFT timed out; compiled tests are authoritative. That checkpoint did not exercise SQLite index persistence or ready-state installation (verified subsequently above); portable semantic/reference validation remains pending. The prior storage benchmark is not a benchmark of this production reader.

- Preparation ownership: normal and race runs of `go test ./internal/epubstore -count=1` pass, including existing acquisition/staging coverage. New synthetic checks cover competing claims (one winner), stale claim/failure rejection after retry, mode snapshot persistence, failure evidence, acquisition independence, deletion cascade, and transaction rollback when a trigger rejects a replacement attempt. AFT inspection timed out; compiled/race tests are the authoritative checks. No ready-state output, startup reconciliation, live schema registration or portable EPUB validation was exercised.

- Receipt acquisition: normal tests for `epubstore`, `readerstore`, `txtstore` and race tests for those packages plus `epub`, `api`, `reading` pass. Fresh isolated homes verify unchanged originals and snapshotted image mode, duplicate-ID rejection, persisted transfer failures, path/policy/cancellation boundaries, idempotent discard/recovery, pre/post-move interruption, size mismatch, retained removal intent, and foreign-path rejection. A SQLite trigger forces the actual post-rename metadata write to fail; recovery then completes the retained intent after removing the trigger. Shared file-writer tests verify exact/over-limit bytes and exclusive creation; existing TXT/API tests protect upload/inbox behavior. AFT has no gopls; compiler/race checks are authoritative. No live schema registration, epoch change, preparation-generation persistence, portable EPUB proof, power-loss simulation or deployment occurred.

- Storage-side staging: `go test ./internal/epubstore -count=1`, race tests for `epubstore`, `epub`, `reading`, and `CGO_ENABLED=0 go test -tags nodynamic ./internal/epubstore -count=1` pass. Synthetic cases cover section JSON, shared cover/prose derivative identity, original-mode no-copy behavior, unchanged inputs, scratch removal, idempotent caller discard, failed-preparation cleanup without deleting sibling attempts, cancellation and final-section output limits. AFT completed but gopls was unavailable; compiled tests are authoritative. No schema/DB, crash-recovery, portable, resource-authorization or real-book claim is made.

- Original-resource read boundary: targeted normal/race verification covers unchanged image bytes without package/section parsing, member-byte limits, missing/invalid references and cancellation. The helper reuses bounded archive indexing/CRC-checked member reads and does not extract files, decode pixels, authorize public requests or establish portable trust. Persistent-resource authorization and restore validation remain required before exposure.

- Preparation integration: `go test -race ./internal/epub ./internal/imageproc ./internal/reading -count=1` and `CGO_ENABLED=0 go test -tags nodynamic ./internal/epub -run 'TestPrepare(Image|Derivative)' -count=1` pass. Synthetic regressions verify once-only conversion across cover/repeated prose, derivative dimensions and caller IDs, unchanged originals, backend evidence separate from review diagnostics, per-image/aggregate limits including cache hits at an exhausted budget, sink/cancellation/empty-ID failures, invalid policy and empty preparation results on failure. Existing original-mode and reading-wire regressions pass after the internal Prepare signature update. AFT lacks gopls; compiler/race tests are authoritative. No full backend suite, new native/container proof, browser flow or real-source check was run for this increment; codec/container verification below remains prior-checkpoint evidence. No user data, schema, dependency or deployment configuration changed.

- Optimizer checkpoint: `go test -race ./internal/imageproc ./internal/epub ./internal/reading -count=1`, `CGO_ENABLED=0 go test -tags nodynamic ./internal/imageproc -count=1`, formatting and whitespace checks pass. Synthetic tests independently decode WebP and cover no-upscale/proportional dimensions, 16-to-8-bit alpha, all eight oriented alpha maps, metadata removal, source ownership, backend evidence, source/output limits, cancellation and invalid profiles. Two conversion tests execute concurrently, without adding production scheduling. No existing module versions were upgraded. AFT has no gopls diagnostics here; Go compilation/tests are authoritative.
- Full local `docker build -t novelreader:epub-imageproc-check .` passes, including frontend build, existing OpenCC/API checks and imageproc tests mounted into the final Alpine runtime with native encoding required. Both success and bounded-output failure paths run against actual native libraries there. No image was pushed, deployed or connected to existing reader data. BuildKit reported missing Git commit metadata, not an application/test failure. Cross-architecture release builds and complete EPUB/browser flows were not run.
- Allocation ownership review: upstream native encode defers WebPPictureFree after initialization, including import/encode/writer failure; portable encoding creates a per-call module and defers freeing input/size/output allocations. Our bounded writer copies accepted bytes into result-owned storage, preserves limit/cancellation errors hidden by the native binding, and never returns partial output. Resize precedes rotation, bounding the additional oriented bitmap to the chosen output dimensions. The module owns no image cache/pool/global buffers. Codec/resampler work can delay cancellation; output-byte bounds are not a cap on codec scratch memory.
- Ignored synthetic lifecycle probe at `reference/imageproc-lifecycle/`: four batches per backend, each 30 successful + 30 output-limit-failed conversions (256×192 input, 128-pixel cap). Native post-GC heap stayed ~548–550 KB and RSS ~12.2–12.5 MB; portable post-GC heap ~506–522 KB and RSS ~9.0–10.2 MB. Neither showed a steadily growing retained trend at this sample size. Measurement-only GC is not used in production. Both complete four-batch reports were written, although the outer build/run command hit its 240-second timeout; no proof container remained running. This is supporting evidence, not a hard memory bound, universal leak proof, or large-image/concurrent-worker stress test.

- Shared validator extraction: normal and race runs of `go test ./internal/imageproc ./internal/epub ./internal/reading -count=1` pass, as does whitespace validation. Shared tests cover complete pixel decoding, declaration/limit/animation/cancellation boundaries and both EXIF byte orders; the EPUB regression preserves original bytes, metadata, cancellation and domain error classes. Existing preparation and wire-contract tests pass unchanged. Validation returns no image or byte reference and has no cache/global pixel state; native allocation/leak testing remains part of the upcoming encoder implementation, not a claim made by these tests. AFT inspection timed out; Go compiler/tests are authoritative. No schema, endpoint, frontend or user-data changes.

- Isolated Go encoder proof (synthetic images only): `github.com/gen2brain/webp v0.6.4` with `golang.org/x/image v0.46.0` Catmull-Rom resampling, WebP quality 92/method 4. The candidate wraps libwebp 1.6.0 and automatically chooses native dynamic libraries or transpiled Go; `nodynamic` forces the latter. Its options expose neither context nor an encoding progress cancellation hook. The later accepted deployment contract permits portable fallback only with explicit affected-import visibility; it must not silently appear equivalent in performance. The isolated module also resolves purego v0.10.1; no application go.mod/go.sum or build files were changed.
- Three iterations per synthetic case verified independent Go WebP decoding, exact alpha-channel retention and expected dimensions: 320×200 remains unchanged, 3840×2160 becomes 2048×1152, and 2048×3072 16-bit PNG becomes 1365×2048 8-bit output. Native Alpine libwebp 1.6.0 encode medians were 9/202/211 ms respectively; complete decode/resize/encode medians by stage summed to approximately 10/488/681 ms. Portable transpiled encoding was 37/1283/1248 ms, with pipeline sums approximately 37/1593/1719 ms. These are small, highly compressible synthetic samples, not predictions for every illustration or the deleted novel-image benchmark.
- The native path was explicitly confirmed by `webp.Dynamic() == nil` in a disposable `golang:1.27-alpine` container with CGO and libwebp-dev. Portable build/run succeeded with CGO disabled and `nodynamic`. Whole-proof peak RSS, including fixture generation and all iterations, was ~308 MiB native versus ~452 MiB portable; it is not isolated per-image or two-worker memory. Go total allocation for the 4K pipeline was ~176 MiB native versus ~522 MiB portable; native C allocations are not counted by Go's allocation counter. No conclusion of zero-cost conversion or bounded process RSS follows. Actual application image, other architectures, orientation/color-management conversion, cancellation latency and generated-output limits remain unverified. Ignored synthetic harness/results: `reference/go-webp-proof/`.
- Accepted deployment decision (supersedes the initial reject-fallback proposal): prefer native libwebp and retain portable fallback, explicitly notifying the user on affected optimized imports. The controlled measurements below justify the small additional native runtime footprint. Keep image processing independent of how that status is presented; expose immutable backend identity from imageproc, record actual backend identity with preparation evidence, and let import API/UI present a non-blocking notice such as “Using the portable image encoder; preparation may take longer.” Original-mode imports require no codec notice. Do not confuse this performance notice with content-loss diagnostics or force content review merely for using fallback. Do not add a runtime encoder selector, a second codec library, or a second worker pool.
- Controlled follow-up: both executables ran sequentially inside the same Go/Alpine container, using the same synthetic inputs, quality 92/method 4 and three iterations. Native versus portable median encoding: small image 9.30/37.46 ms; resized 4K 198.35/1286.18 ms; resized 16-bit portrait 229.73/1580.59 ms. Full 4K decode/resize/encode stage medians summed to 488.52/1579.34 ms (~3.2× improvement). Whole-proof peak RSS was 314620/532004 KiB (~41% lower with native); fixture generation is included, and this is not a concurrent-worker or hard RSS budget. Dimension and alpha assertions passed for both paths. Output sizes matched; no claim of universal byte-identical encoder results or physical low-end-device latency.
- Minimal Alpine 3.24 runtime proof: libwebp 1.6.0-r0 (545 KiB), libwebpdemux (21 KiB) and transitive libsharpyuv (25 KiB), ~591 KiB installed package size beyond the existing libc. Without development packages, loading initially failed because the binding requests `libwebp.so` / `libwebpdemux.so` rather than installed versioned sonames. Adding `/usr/lib/libwebp.so -> libwebp.so.7` and `/usr/lib/libwebpdemux.so -> libwebpdemux.so.2` made the dynamic availability assertion pass. Production Docker integration must provision the runtime packages/links and test actual native encoding in the final-image environment, not merely the builder. Do not install libwebp-dev or a compiler in the runtime image. Verify selected sonames with package updates. The application already has a dynamically linked CGO/OpenCC build; CGO-disabled static builds can use the portable path with notice. These proof-only containers did not change production Dockerfile or publish any image.

- Limit-sized image measurement: isolated non-race `ValidateImage` children on this host decoded synthetic 8192×4096 images at the provisional 32 Mi-pixel cap. 8-bit RGBA PNG: 135,552 KiB peak RSS / 211 ms; 16-bit RGBA PNG: 266,880 KiB / 574 ms; JPEG: 53,568 KiB / 216 ms. Four sequential 16-bit PNG validations reached 529,920 KiB peak RSS / 1,930 ms without forced collection between calls. Post-GC heap returned to within 8 KiB of baseline while encoded bytes stayed live: no retained-bitmap cache was found. This measures decoder/runtime high-water memory, not full-book or concurrent-worker memory. The planned shared pool has two workers, so the current cap is consequential on small hosts. Accepted decision: retain the 32 Mi-pixel cap rather than reject more books based on this extreme synthetic case. Oversized referenced images still abort preparation with ErrLimit. The later accepted optional optimization design adds bounded transcoding during preparation; it does not reduce this original-decode peak. Do not add a separate memory scheduler or forced garbage collection. These import-time costs are not normal chapter-reading costs. Ignored reproducible helper, synthetic fixtures and raw evidence: `reference/epub-image-memory-proof/`.

- Auxiliary bookmark reopening: `npm test -- src/features/reader/ReaderView.navigation.test.ts src/features/reader/ReaderBookmarksSheet.test.ts src/features/reader/reader-session.test.ts` passes (3 files / 23 tests), as do frontend typecheck and scoped zero-warning ESLint. `go test ./internal/reading ./internal/library -count=1` passes for existing location-validation and library state invariants. No production or schema change was needed. AFT inspection timed out; compiler/test verification completed independently.
- Focused EPUB 2 source check: OPF §§2.3.1, 2.4 and 2.6 at the IDPF reference below agree with following manifest fallback chains, spine-order primary reading, `linear="no"` auxiliary membership, discovery of linked unlisted documents as auxiliary, separate hierarchical NCX targets, and whole-document versus fragment guide scope. The served document contains editorial redlines; this is not a claim of complete EPUB 2 conformance. NovelReader deliberately narrows spine rendering to XHTML (not DTBook/CSS XML islands), ignores NCX pageList/navList in this milestone, and uses a review-required spine list for absent/unusable TOCs. Remaining profile/error-category finalization is still required.

- Cover placeholder: EPUB/reading package tests pass, covering emitted marker ownership and anchor-preserving projection without invented prose/resources. Targeted renderer/parser/conversion/i18n tests pass (4 files / 18 tests), production build/typecheck and scoped zero-warning ESLint pass. Actual renderer browser checks at 1280/390px pass for existing artwork, localized caption, retained anchors, hidden-image caption-only state and no overflow/page errors; ignored evidence is `reference/epub-cover-proof/`. AFT inspection could not complete due to transport failure; compiler/tests are authoritative. No schema, provider exposure or existing-data change.
- Representative payload/cache measurement (Chromium 152, this host, Vite dev build, mocked transport): 100 rich paragraphs = 38,720 wire bytes / 401 semantic nodes; 5,000 paragraphs = 1,939,020 bytes / 20,001 nodes. At 1280/390px, cold fetch/parse took 4–7 ms / 61–76 ms respectively; mount plus two animation frames took 14–26 ms / 258–268 ms. Warm load reused object identity in 0–0.1 ms without another request. Loading six sections evicted the oldest from the existing five-entry cache, and revisiting it fetched again. After forced GC, five large cached sections increased JS heap by ~16.9 MB over pre-run baseline; disposal reduced that to ~5.3 MB (includes runtime/render/JIT overhead, not a retained-cache size claim). No evidence here justifies a new cache owner, byte-budget tuning or section splitting. This is one synthetic run per size/viewport, not a physical mobile benchmark, production latency budget, decoded-image memory check or parser-cap worst case. Reproducible ignored harness/raw results: `reference/epub-payload-proof/`.

- Image orientation check reproduced and resolved a layout defect with a synthetic 480×240 JPEG carrying EXIF orientation 6. Actual ProseRenderer in Chromium reserved 480×240 before decoding, then rendered 480×960 (natural oriented dimensions 240×480). At 390px viewport width it shifted from 374×187 to 374×748. An otherwise identical unrotated JPEG retained its geometry. `ValidateImage` now supplies orientation-aware display dimensions. Using actual Go-validated fixture dimensions, both rotated JPEG and PNG reserve and display 240×480 without a load-time geometry change at desktop/mobile widths; the plain JPEG remains responsive at 480×240 / 374×187. `go test ./internal/epub ./internal/reading -count=1` passes, including both EXIF byte orders, orientation-axis groups and truncated/out-of-range metadata. No frontend or dependency change. AFT completed but has no Go diagnostic producer (`gopls` unavailable); Go tests are authoritative. Local synthetic evidence/harness: `reference/epub-image-proof/`. This is a measured geometry check, not a performance benchmark.

- Structured transport/composed-reader checkpoint: `testdata/epub-reading.json` is checked against actual synthetic EPUB preparation and Go catalog/content projection, then consumed through frontend `getChapterContent`. It covers the main/auxiliary inventory, note target/anchor coherence, image media/dimensions, zero-based list start, table spans and fallback prose. `go test ./internal/reading -count=1` passes; frontend reader/API tests pass (22 files / 84 tests), production build/typecheck and scoped zero-warning ESLint pass. Unknown versions and malformed version-2 nodes fail without legacy fallback. At 1280×900 and 390×844, real ReaderView/loader/renderer with mocked API responses pass keyboard note activation, anchor focus, nested return and actual scroll restoration, TOC auxiliary selection, table keyboard/pointer isolation, no auxiliary progress requests, warm section reuse, hidden-image zero requests and explicit image display, with no horizontal overflow/page errors. Final run uses production modules without the preliminary parser hook. Ignored reproducible harness/evidence: `reference/epub-reader-proof/`; its catalog/prose extend the shared synthetic wire fixture for long scrolling, wide tables and nested visits. This is not a real backend, authorization, bookmark-persistence, last-read or throughput proof. AFT inspection timed out; compiler/tests are authoritative.

- TOC presentation increment: affected frontend tests pass (22 files / 83 tests), including shared-list search/qualified targets/inactive states, Book Detail propagation, ReaderView auxiliary selection/return and main-progress isolation, and canonical-text-preserving cached label conversion. Production build/typecheck and zero-warning scoped ESLint pass. Synthetic Playwright checks at 1280×900 and 390×844 pass for Book Detail links, expanded groups/unavailable ancestors, long labels, search, keyboard activation, centered current-row jump, Escape, and no horizontal overflow/page errors. A fresh Vite process was required because WSL file watching served stale modules; the final browser run checks the new section-summary copy explicitly. Ignored evidence is under `reference/epub-toc-proof/`. No backend/schema/data changes or live EPUB admission in that increment; the later composed ReaderView proof is recorded above. AFT diagnostics did not complete (transport timeout); compiler/test checks are authoritative.
- Catalog navigation increment: the shared synthetic wire fixture `backend/internal/reading/testdata/epub-navigation.json` is checked against Go EPUB 2/3 projection and consumed through frontend catalog transport. Focused `go test ./internal/reading -count=1` passes; frontend parser/API tests pass (3 files / 12 tests) plus typecheck. Covers hierarchy, repeated section anchors, authored order distinct from spine order, unavailable parents with reachable children, optional legacy serialization, fallback provenance, stale/missing/volume/conflicting targets, and depth/count limits. Shared ReadingTarget parsing now owns the revision rule for both prose links and navigation; no private preparation types/paths cross this contract. AFT timed out; compiled/tests checks remain authoritative. Broader affected-area verification also passes: `go test ./internal/reading ./internal/api -count=1`; frontend reader/catalog/BookDetail tests (22 files / 83 tests), production build/typecheck and zero-warning scoped ESLint. No browser, live provider, schema or existing-data change in this increment.
- Reader lifecycle increment: targeted frontend tests pass **21 files / 78 tests** across reader, reader/structured API and i18n; production build/typecheck and scoped ESLint pass. Coverage includes nested same-main notes, refresh write exclusion, failed/superseded proposals, scoped anchor restoration with rollback, direct note visits, qualified link intent, mixed legacy/structured conversion-cache reuse and focused link/button/table keyboard behavior. The final navigation/i18n rerun also passes (2 files / 20 tests) after focus restoration and compact Return labels. Geometry/frames are controlled in the integration fixture; this is not a real-browser note workflow. No backend tests or new browser run for this frontend increment. AFT timed out; TypeScript/Vitest/build are authoritative.
- Untitled-bookmark correction: reproduced HTTP 400 for a valid untitled section, then passed `go test ./internal/api -run '^TestBookmarkAPI' -count=1` and `go test ./internal/reading ./internal/library -count=1`. The regression covers exact repeated-ID retry, stored location/empty title, volume/missing-section rejection and unchanged resume/last-read with only one state-version increment. Frontend bookmark-sheet tests pass (1 file / 2 tests), plus typecheck and scoped ESLint; the new case verifies numeric display fallback and exact revision-qualified reopen event. No browser, build or additional race run for this localized correction. AFT timed out; compiled/tests checks are the verification authority.
- Membership/navigation increment: `go test ./internal/reading -count=1` and `go test -race ./internal/reading ./internal/api ./internal/library -count=1` pass. Covers prepared inventory projection, exact legacy catalog serialization, main/auxiliary progress-versus-bookmark location validation and existing API/library behavior. Frontend targeted Vitest (`src/features/reader`, reader/structured-prose API tests and BookDetailView) passes **21 files / 72 tests**, production build/typecheck and scoped ESLint pass. Covers malformed/duplicate catalog membership, interleaved auxiliary skips, auxiliary ReaderView no-write/prefetch/bookmark capture, nested same-main/auxiliary note return proposals and stale/unavailable targets. No new browser or full EPUB lifecycle check was run for this increment. AFT again timed out during Tier-2 analysis; compiled/tests results are authoritative.
- Frontend `npm run typecheck`, `npm test -- src/features/reader src/api/reader.test.ts src/api/structured-prose.test.ts` (19 files / 61 tests), production build and scoped ESLint pass for the candidate parser, common renderer and conversion. Coverage includes finite safe DOM/text, unchanged legacy rendering, revision-qualified note actions, scoped anchor lookup, image failure/hiding, immutable text conversion and resource/action/depth rejection. The style detector returned no scoped findings. The latest AFT inspection timed out during Tier-2 analysis; TypeScript/Vitest/build are authoritative here.
- Isolated synthetic Vite/Playwright proof passed at 1280×900 and 390×844: no horizontal page overflow or page errors, wide tables scroll locally, literal script-like text remains text, block flow is preserved, PNG reserves/renders 4:3 at 640×480 and 342×256.5 respectively, and Enter activates a revision-qualified note event. Resource delivery was intercepted with a generated PNG; no reader account/data or backend EPUB endpoint was used. Screenshots and the local harness are ignored under `reference/epub-renderer-proof/`; the temporary frontend entry is removed. The CLI Chromium launch failed in crashpad; the successful proof used the installed Playwright headless shell. This is not note-return, authorization, EXIF-orientation, cold/warm cache or large-section performance proof.
- `go test ./internal/reading ./internal/epub -count=1` and `go test -race ./internal/reading ./internal/api ./internal/epub -count=1`: preparation-to-reading integration with main/note membership, block versus inline flow, figure/caption and ruby/list/table semantics, language/direction, revision-qualified note targets, explicit external/unavailable links, opaque image references with validated media/dimensions, safe unsupported-content fallback, canonical list-state isolation, private-data exclusion, missing-binding/unknown-kind/cancellation rejection and exact version-1 wire compatibility. Existing API regressions also pass. This is backend contract evidence, not a renderer or authorized-resource proof.
- `go test ./internal/epub -count=1` and `go test -race ./internal/epub -count=1`: synthetic package/order/reference cases, archive and XML boundaries, cancellation, EPUB 2 declarations/entities/UTF-16, NCX/nav hierarchy and selection, mixed labels/group headings, repeated-document fragments, spine independence/non-linear targets, unavailable targets, optional-navigation diagnostics, base overrides, fatal navigation byte/depth/entry/cumulative limits, fixed-layout declarations, font obfuscation versus encryption, spine fallback/cycle/missing-file handling, consistent versus conflicting resource aliases, semantic structure/whitespace, opaque/private reference separation, safe/inactive links/images, ambiguous anchors, SVG raster wrappers, bounded section normalization, complete static-image validation versus false declarations/corrupt pixels, animation/dimension/byte limits, repeated-resource deduplication and readability after missing images, forward/self/note-return targets, literal decoded-fragment matching, unavailable/ambiguous targets without fallback, resolved-tree/navigation privacy, repeated-resource occurrence handling and target-index ownership/limits/cancellation, full staged preparation, interleaved non-linear spine membership, cyclic auxiliary discovery, unused-resource exclusion, bound image metadata across staging, explicit cover declarations/placeholder admission, unreadable primary-content rejection, fatal auxiliary limits, output/cancellation/occupied-scratch/short-write failures, stage budgets, large-section completeness, UTF-8 reference integrity, cover declaration precedence/alias/conflict handling, explicit-page fallback without reading-graph changes, prevention of surviving-decoration misselection and fatal cover-validation failures. Default runs skip the explicitly opt-in local fixture.
- `NOVELREADER_EPUB_FIXTURE="$(pwd)/../test-epubs/全职高手 (蝴蝶蓝).epub" go test ./internal/epub -run '^TestLocalPackageInspection$' -count=1 -v`: package support/inventory/navigation only; logs counts and diagnostic codes, not title/text/hrefs/assets. Use an absolute fixture path because Go tests run in their package directory; an earlier relative-path invocation failed before reading the book.
- `NOVELREADER_EPUB_FIXTURE="$(pwd)/../test-epubs/全职高手 (蝴蝶蓝).epub" go test ./internal/epub -run '^TestLocalSectionNormalization$' -count=1 -v`: full spine section normalization, referenced static-image validation and navigation binding against the completed target index; logs counts/diagnostic codes, not book content or original references. Sections are not retained together. The target proof requires readable sections; it does not guess cover-admission policy or silently omit unreadable spine entries.
- `NOVELREADER_EPUB_FIXTURE="$(pwd)/../test-epubs/全职高手 (蝴蝶蓝).epub" go test ./internal/epub -run '^TestLocalPreparation$' -count=1 -v`: full staged parser preparation, emitting/counting one tree at a time; repeated successfully after the flow-semantics correction. Recorded cover-selection checkpoint (`b269f15`) measurements: 1,737 main sections, zero auxiliary sections, 3,472 image bindings, 25,582,370 tree JSON bytes, 25,921,003 scratch bytes, an embedded cover selected and no diagnostics (2.40 seconds at that checkpoint). Links/auxiliary behavior is synthetic-test coverage, not evidence from this particular book.
- Focused process measurements recorded at staged-preparation checkpoint `97cdb41` (before cover selection): compiled a non-race `go test -c` binary and ran each proof in a fresh child, using Python standard-library `os.wait4` peak RSS and wall time (system `/usr/bin/time` is unavailable). Go 1.27.0 linux/amd64: normal synthetic staged proof **7,488 KiB / 0.010 s**; 1.17 MB, 10,000-paragraph single-section proof **31,276 KiB / 0.088 s**; local-book staged proof **24,464 KiB / 2.415 s**. These include test/runtime and synthetic fixture construction, not isolated parser heap measurements or a comparative benchmark. The local callback also serializes emitted trees for byte counts. The local book repeats small raster resources; this does not measure maximum-sized image decoding or browser layout/cache costs.
- `go test ./internal/epub -run '^$' -bench '^BenchmarkNormalizeSection$' -benchmem -count=1`: normal synthetic section 18,551 ns/op, 17,968 B/op; 1.1 MB / 10,000-paragraph single document 31,480,811 ns/op, 28,496,645 B/op, on the same Go/CPU below. Construction excluded; allocations are not peak memory, and this excludes image decoding, target binding, persistence and rendering.
- `go test ./internal/epub -run '^$' -bench '^BenchmarkInspect$' -benchmem -count=1`: synthetic metadata inspection with an approximately 19 MiB single-spine body took 46,485 ns/op, 26,295 B/op, 376 allocations/op on AMD Ryzen 9 5900X, Go 1.27.0 linux/amd64. Fixture construction is excluded; chapter bytes are not decoded. This is not peak memory, full preparation, rendering performance or a comparison with Readium.

Navigation implementation consulted the EPUB 2 OPF NCX requirements at <https://idpf.org/epub/20/spec/OPF_2.0.1_draft.htm> and EPUB 3.3 navigation restrictions/TOC definition at <https://www.w3.org/TR/epub-33/>. Earlier reference-reader research is linked above. The earlier backend scoped AFT inspection completed, but lacked authoritative Go diagnostics because `gopls` is unavailable. Focused Go tests/race compilation are the authority. The metadata benchmark above predates navigation/support parsing and was not rerun for this slice.

Not performed: authorized cover/resource serving and shelf integration, full-book/concurrent-worker/browser-decoded-image memory measurements, integrated EPUB reader/browser validation, end-to-end auxiliary bookmark/resume integration and browser-confirmed same-main note UI write isolation, durable EPUB storage/backup/restore, schema cutover, complete local-book compatibility, container build or hosted CI. Checkpoint 1 remains unfinished.

### Portable preparation ownership checkpoint

Accepted: portable copies clear installation-local preparation staging names, fail interrupted running preparations for explicit retry, and preserve queued intent and installed/finalizing output. A finalizing generation with no local staging name may recover only from its installed directory; absent output fails for retry, without consulting temporary work. Completed output is preserved rather than discarded merely because finalization was interrupted. The cleanup hook acts only on copied databases and must be idempotent. This checkpoint remains unregistered; full EPUB portable reference/semantic validation and application integration are still required before epoch activation.

Current state: the private `preparePortable` hook and recovery adjustment are implemented. The existing readerstore snapshot/replacement hooks are reused; there is no new lifecycle layer. Isolated snapshot/replacement tests verify source-state preservation, stripped staging names, repeated-hook stability, running-work failure, queued-intent preservation, installed-output recovery and missing-output retry, with original bytes preserved. The hook is registered only in tests; full portable reference/semantic validation remains next. No live home, archive or application schema was altered.

### Portable ownership validation checkpoint

Implemented a read-only receipt/preparation ownership check using the existing `(context, SQL transaction, confined file root)` validation boundary. Check canonical receipt identity/name/policy, required acquired originals, lifecycle-appropriate missing/damaged originals, current generation membership, and immutable preparation policy. Reject local staging authority and running work in prepared portable copies. Preserve failed historical attempts and interrupted finalizations; do not parse original archives, decode images, repair rows, or sweep files. This component is not the complete portable validator: section/resource/semantic and eventual publication checks remain required before registration. Isolated snapshot/replacement tests now run cleanup plus ownership validation. Focused negative cases cover required originals, generation membership, immutable policy, retained local staging/running state and orphan preparations; interrupted acquisition/removal allowances and failed attempt history remain portable. No archive parsing, image decoding, new runtime abstraction or live schema registration was introduced.
