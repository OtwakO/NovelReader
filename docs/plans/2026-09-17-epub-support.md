---
status: in-progress
updated: 2026-09-17
---

# EPUB support: structured novel reading

## Goal

Import a DRM-free reflowable EPUB, review it, add it to the existing shelf, read its text and illustrations with meaningful formatting/navigation, resume and bookmark it, and back it up or remove it through the existing reader-owned lifecycle.

This is a structural milestone, not a second reader application. Done means a reliable, explicitly bounded novel-reading profile, not universal EPUB reading-system conformance.

## Accepted direction and authorization

The user selected structured reflowable EPUB 2/3 reading and both browser and server-inbox import. After reviewing the proposal and reference research, the user authorized implementation, starting with the bounded contract proof. Keep existing reader homes/backups untouched. Schema cutover remains a separate gate before storage changes; checkpoint 1 does not change the schema.

Follow [reading documents/resources decision](../decisions/0002-reading-documents-and-resources.md). Reuse the library, reading session and prose modality. Do not create a plugin framework or an EPUB-specific reading stack.

## Scope and why

### Include

- Browser uploads and explicit server-inbox selection within Local Import; mixed TXT/EPUB selections retain independent outcomes.
- Package metadata, embedded cover, ordered reading sections, EPUB 3 navigation and EPUB 2 NCX navigation.
- Paragraphs, headings, emphasis, explicit line breaks, lists, quotations, illustrations/alternative text, internal links and footnotes. Include ruby annotations and simple semantic tables in the content-contract checkpoint: these carry meaning in novels and should not be silently concatenated into plain text. No CSS layout emulation.
- Existing typography, image visibility, Chinese conversion, progress, last-read tracking, bookmarks and revision-qualified navigation.
- Default-on review preference; disabling it may automatically accept only an eligible warning-free initial import from current client work, never a recovered receipt or a degraded book without approval.
- Review metadata, contents and a bounded sample; clear unsupported-content warnings; explicit acceptance, retry/discard and persisted history.
- Reader isolation, crash recovery, bounded resources, portable backup/restore and complete removal/cleanup retry.

### Exclude

- DRM decryption, fixed-layout/mixed fixed-layout publications, publisher CSS/fonts, scripts, forms, audio/video/media overlays, full SVG/MathML rendering, vertical-layout fidelity and exact printed pagination.
- EPUB editing/export, replacing a published original, published reparse, version history, cross-provider merging, content-hash deduplication, full-text search and downloadable offline packages.
- EPUB CFI or a universal location framework. Existing section/progression persistence remains the default; exact internal-link targets are a separate navigation concern.
- Automatic remote resource retrieval, archive extraction trees, a rendering browser service or client-side archive parsing.
- User-configurable cover overrides. The user left this as a possible later shared-library feature across all providers, not part of EPUB implementation.

These exclusions narrow fidelity, not data ownership: preserve the original unchanged. State the supported profile before upload/review; do not advertise complete EPUB compatibility.

### Direction assessment

**Why not text-only conversion?** It would be cheaper initially, but lose emphasis, reading order distinctions, notes and intra-document navigation. Repairing that after publication could change locations and require reinterpretation. It does not satisfy the selected scope.

**Why not embed a full EPUB browser reader?** It can preserve publisher layout better, but adds CSS/DOM isolation, archive/resource delivery and a separate navigation/location lifecycle. That is justified for publisher fidelity, not this novel-focused milestone.

**Why both intake channels?** They are two ways to acquire the same durable original, not two parsers. Browser-first is a useful implementation checkpoint, but server-inbox parity belongs in milestone completion. Reuse existing inbox safety and explicit cleanup proof; do not implement a second independent intake scheduler.

**Main cost:** semantic documents and navigation, plus lifecycle integration. ZIP/XML decoding alone is not the milestone. The first checkpoint must exercise these costs before committing to detailed persistence.

### Reader references and performance

The user clarified that original publisher layout is unnecessary and efficiency/performance matter. [Legado/web-legado/Readest findings](../notes/2026-09-17-epub-reader-reference-lessons.md) support app-owned responsive typography while preserving semantics. Readest's polished reading layout does not require adopting its Foliate browser-rendering stack; Readium Go was evaluated separately as a package-parsing candidate.

- Prepare and persist semantic sections once, processing one section at a time. Ordinary reading must not download, inflate, parse or mount the whole book.
- Keep images separate and lazy; preserve validated dimensions where available to reduce layout shift. Reader style changes reflow local content, not trigger reimport.
- Reuse the shared chapter loader's request deduplication and bounded recent cache/prefetch. Check whether rich-section byte limits or byte-weighted retention are needed; do not create an EPUB-only cache owner.
- Include a large single-spine document in the first proof. A section can contain much more than a conventional chapter; do not assume five cached sections means small memory, or add speculative splitting/virtualization before measuring.
- Preserve the current reading mode. Use fluid single-column phone text and comfortable desktop measure; no new full-book pagination, dual original/parsed modes or unrelated reader redesign in this milestone. Borrow Readest's quiet controls and contextual navigation, not its entire feature set.
- Record import time/peak memory, prepared size, cold/warm section payload/load and image layout movement on a small normal fixture and a large-section/image-heavy fixture. These focused observations inform limits and parser choice; no benchmark framework or unmeasured speed claims.

## Evidence from the current code

- `backend/internal/reading/service.go`: a private provider interface already separates catalog/open/location lookup; `resolve` chooses BookSource or TXT. Add one concrete EPUB adapter here, not registration machinery.
- `backend/internal/reading/document.go` and `frontend/src/api/reader.ts`: current backend providers still emit version 1 (paragraph/image blocks). The frontend transport now strictly admits both versions after cross-language and composed-browser proof; no EPUB provider is exposed.
- `frontend/src/features/reader/ProseRenderer.vue`: one app-owned semantic renderer now serves legacy adapters and candidate structured documents; no publisher HTML or second EPUB renderer.
- `frontend/src/features/reader/chinese-conversion.ts`: pure conversion now supports structured text without changing canonical documents or identifiers/targets. The shared loader/memoization path handles both supported document versions.
- `backend/internal/library/schema.go`: shared progress/bookmarks use chapter index, normalized position and content revision. No evidence yet requires replacing that persistence model.
- `backend/internal/txtimport/pool.go` and `work.go`: scheduling already has a narrow processing seam, but construction, errors and recovery are TXT-bound. Generalize the real scheduling owner, not TXT's parser/state tables.
- `backend/internal/txtstore/receive.go`, `portable.go`: acquisition is distinct from publication; originals and durable intent participate in the reader-home mutation/snapshot lifecycle. Portable validation does not rebuild content.
- `frontend/src/features/imports/import-queue.ts`: current orchestration and DTOs are TXT-specific. Share queue lifecycle with typed format-specific operations rather than copying the queue or making every receipt carry TXT options.

Format checks against [EPUB 3.3](https://www.w3.org/TR/epub-33/): [spine itemref](https://www.w3.org/TR/epub-33/#sec-itemref-elem) distinguishes primary and auxiliary reading; [TOC navigation](https://www.w3.org/TR/epub-33/#sec-nav-toc) is a hierarchy of targets, not one entry per spine item; [font obfuscation](https://www.w3.org/TR/epub-33/#sec-font-obfuscation) is distinct from DRM. EPUB 2 package/NCX rules still need focused primary-source checking during the contract checkpoint.

## Accepted architecture

### Ownership and flow

```text
Local Import (browser / inbox)
  → shared bounded intake and worker lifecycle
  → EPUB-owned durable receipt + unchanged original
  → bounded EPUB parser/normalizer
  → saved sections, navigation, resource/anchor mappings and diagnostics
  → explicit library publication transaction
  → existing reading service + EPUB adapter
  → versioned semantic prose + authorized opaque resources
  → shared Reading Session / Prose Renderer
```

- `internal/epub`: bounded container/package inspection, URL resolution and semantic normalization; no HTTP, library mutations, home paths or worker ownership. **Selected after the build proof:** focused EPUB rules over Go ZIP/XML and existing HTML/text primitives, with no new dependency. Readium's package-level coupling brings unrelated cloud/gRPC/telemetry dependencies even through its narrower exported parsing functions. The user chose the focused Go approach after reviewing that measured cost. Do not fork Readium, maintain parallel parsers or expose archive references in HTTP/storage-facing prose documents. Format-rule ownership and conformance tests are the cost of this choice.
- `internal/epubstore`: original, durable import state, prepared content/index, publication and provider-owned removal/recovery/portable validation. Library alone owns display metadata, progress, bookmarks and revisions; parsed metadata before admission is import evidence, not a second authoritative shelf record.
- Evolve `txtimport` into a small local-import lifecycle owner as EPUB lands. One shared process budget and reader quiescence barrier; concrete TXT/EPUB dispatch with fair selection so continuous TXT work cannot starve EPUB. No dynamic registry, generic task engine or extra independent worker pool.
- Share confined transfer/finalization and inbox claim mechanics only where both formats have the same invariant. Keep encoding/regex/reparse and archive normalization in their respective owners. Do not rename/rebuild every TXT table as a prerequisite.
- HTTP owns authentication, DTOs and safe response headers. Reading service selects the provider. Renderer receives semantic content, never ZIP paths, OPF metadata or provider parsing instructions.

### Sections, TOC and footnotes

1. Main sequence follows the package spine, not ZIP order, manifest order or TOC order. One supported spine content document is one section initially; no heuristic splitting by headings.
2. Navigation is distinct: preserve hierarchy and map entries to section plus opaque anchor. Several entries may target one section. Extend catalog output with optional navigation rather than duplicating sections or overloading `isVolume`.
3. Normalize document IDs to opaque anchor keys during preparation; links carry validated publication-local targets qualified by the same content revision. No archive href reaches the client.
4. Auxiliary/non-linear and referenced note documents remain reachable but do not become ordinary next/previous or prefetch steps. Accepted representation: indexed sections with an optional `auxiliary` flag; omission means main membership for existing catalogs. Main order is catalog/spine order with auxiliary sections skipped, never an ordinal prefix. Keep section ordinals stable within the published interpretation.
5. Use ordinary note navigation with a visible return action before considering popovers. The Reading Session retains a step-by-step return stack for nested note visits, including notes within a main section; returning restores the prior visit's progress eligibility and location. Ordinary deliberate chapter navigation exits note context. Do not create a second independent reader for notes. Auxiliary views must not overwrite the main resume/last-read position. Accepted: allow auxiliary bookmarks and reopening them, while auxiliary reads and bookmark operations must not overwrite main resume/last-read. Test that behavior in the first checkpoint rather than accidentally accepting indices that shared validation cannot restore.
6. Keep normalized progression for ordinary resumes/bookmarks. An anchor jump resolves after document display, then ordinary main-section progress can be saved. No claim of exact text-position restoration after typography changes. A persisted anchor locator is a separate decision only if the checkpoint demonstrates a real failure of this contract.
7. Missing optional TOC: generate a clearly labeled section list from the spine with review warning. Broken individual targets remain visible as unavailable or produce a review diagnostic; never silently redirect to another chapter. Missing primary readable content is a failure, not a successful partial book.

### Semantic prose and assets

- Define a finite prose schema: supported block types and a small inline vocabulary (text, emphasis, links, ruby, line breaks). Lists/tables have bounded semantic structure, not arbitrary tags/attributes/styles.
- Proposed wire compatibility: add version 2 for richer documents; retain version 1 BookSource/TXT parsing while both are served. Do not silently send new blocks under version 1. Frontend parsing and renderer cases are exhaustive; unsupported versions fail explicitly.
- Preserve language/direction where needed for correct text semantics. Publisher font metrics, generated CSS content and visual placement are not preserved. Unsupported meaningful content yields diagnostics, not silent disappearance; meaningful fallback text remains readable where available.
- Render Vue-owned elements and text; no raw publisher HTML via `v-html`. Conversion visits text leaves/labels without changing opaque IDs or target references. Links/buttons must not trigger reader tap-navigation accidentally.
- Issue opaque publication/revision-bound image and cover resources. Authorization and removal/restore lifetime checks remain shared; archive lookup is EPUB-owned. Validate media type and byte/dimension bounds; never serve arbitrary archive contents as same-origin active content.
- Start with supported raster images. Do not ship a general SVG sanitizer/rasterizer. Common SVG wrappers containing a supported image can normalize to that image only through a narrow parsed rule; unsupported standalone vector art is reported. A cover failure need not make readable prose unusable.
- No remote fetch for referenced publication resources. Accepted: retain HTTP/HTTPS external links for deliberate opening in a separate tab with opener isolation; never fetch them in preparation or prefetch. Other schemes remain inactive. Transport/renderer integration must enforce this policy.

### Storage and lifecycle

- Keep one immutable `original.epub` under the reader's managed files and disposable intake work separately. Read validated ZIP entries directly; do not extract arbitrary archive paths into a directory tree.
- Persist normalized section documents plus navigation/anchor/resource mappings during preparation. This makes published semantics stable across restarts/parser updates and avoids whole-book parsing on each chapter request. Original image bytes stay in the archive; do not duplicate all binary assets.
- EPUB owns a small receipt/result model, not TXT's active/candidate reparse model. Allocate an attempt generation before work; a completed index and diagnostics become visible atomically. Publication uses the exact reviewed attempt and is idempotent. Published content is immutable in this milestone.
- Receiving, queued/processing, ready/review-required, failed, published and removing are lifecycle concepts; exact tables/constraints and legal states are to be written at the storage checkpoint, not copied mechanically from TXT projections.
- Long archive parsing happens outside SQLite write transactions and mutation gates. Finalization/publication use short guarded transactions; canceled or superseded work cannot install results. Incomplete prepared rows/files remain hidden and have explicit cleanup/recovery ownership.
- Recovery requeues interrupted unpublished work only while quiescent. Removal hides the publication and retires work before deleting owned bytes; incomplete cleanup is observable/retryable. Restore, deletion and shutdown drain transfers/work/resources under existing reader-home ownership.
- Backups include original and authoritative prepared content/mappings consistently. Validate bounded stored documents/references and required originals, without re-normalizing an EPUB or running content during restore. Do not export inbox cleanup authority. Pending/failed/removing states need their own legal missing-file rules.
- A new schema epoch is expected (14 if 13 remains current). **Cutover requires confirmation before production edits:** retain old homes/backups untouched, verify fresh isolated homes, no migration or automatic reset. Rollback uses the prior application with its compatible old home; never open a new epoch with the old app. Check the current epoch at implementation time.

### Practical input protections

EPUB is an untrusted archive, so these are necessary boundary checks, not a general security overhaul:

- Bound compressed input, central-directory entry count, total declared and actually read uncompressed bytes, per-entry bytes, document/nesting/node counts, sections and resources; enforce cancellation/deadlines while decompressing/parsing, not just ZIP header claims.
- Reject ambiguous duplicate normalized names, escaping/absolute paths, unsupported encryption/compression and broken required references. Resolve URLs relative to their owning package/document; allow legitimate internal `../` references only when the resolved path stays within the archive. Decode consistently before lookup; fragments are not entry names.
- Do not resolve external XML entities/DTDs or fetch schemas. Handle normal EPUB 2 declarations using a bounded parser policy, not network access.
- Do not classify every `encryption.xml` as DRM: ignored obfuscated fonts do not require decryption. Encrypted required reading content is unsupported; expose a safe category.
- Validate the package's fixed-layout declarations and spine overrides, not only the filename. Unsupported primary content fails or requires a supported declared fallback; do not silently skip primary sections.
- Stable client error categories and bounded diagnostics; raw paths/parser details stay out of public responses. Numerical limits are named constants chosen at the first checkpoint, not new knobs for every theoretical case.

## Delivery checkpoints

1. **Contract proof, before broad plumbing — in progress.** Dependency gate complete: Readium compiled successfully, but the user selected focused Go parsing after its actual package graph was measured (see Current State). Bounded archive/XML, package inventory/navigation inspection, package support checks, isolated section normalization, static image binding validation and registered-section target binding, staged preparation and embedded cover selection are implemented; the complete parser-to-reader design is not yet accepted as proven. Initial preparation measurements include a large single-spine document; browser behavior and worst-case image costs still need proof. Build minimal synthetic EPUB 2/3 fixtures containing a main sequence, multiple TOC anchors in one file, cross-document notes and illustrations. Validate parser/semantic document/renderer/navigation together. Freeze the version-2 schema, section/TOC/auxiliary rules, semantic support matrix, limits and exact failure categories. Check EPUB 2 primary references. If this requires a fundamentally different location or renderer model, stop and revisit scope rather than stacking exceptions.
2. **Durable provider and lifecycle.** Write exact DDL/legal-state table after cutover approval. Implement original acquisition, preparation, generation guards, publication, reading/resources, portable validation and removal/recovery. Extract the shared import scheduler/transfer seam with TXT behavior preserved. Verify at storage/reading boundaries before exposing unfinished imports.
3. **Product integration.** Browser and inbox EPUB routes, one Local Import queue/history/review flow, cover/detail/shelf capability wiring, semantic reader/TOC/note integration and existing preference behavior. Preserve TXT-specific controls only for TXT. Do not expose EPUB reparse controls.
4. **Composed verification and documentation.** Fresh isolated real-server upload and inbox journeys, read/link/note/resume/bookmark, backup/restore/removal, plus focused TXT/BookSource regressions. Update current usage/architecture docs when functionality actually lands. Complete the plan only at the recorded verification scope.

Each checkpoint is a cohesive tested commit or small sequence, not permission to expose a half-working EPUB workflow. Keep this plan's Current State/Next Action/Verification current at meaningful stopping points.

## Verification strategy

Use a few generated synthetic archives rather than a large copyrighted corpus. Fixtures cover format contracts, not special-case book names.

- Parser: EPUB 2/3 normal case plus one feature-dense semantic/navigation fixture; table-driven hostile archive/reference/limit cases exercising real boundary protections.
- Storage/lifecycle: exact reviewed publication, canceled/interrupted work, cleanup retry, cross-reader denial, portable round trip and quiescent restore. Reuse shared intake tests; add only EPUB-specific and mixed-format scheduling cases, not a copy of every TXT test.
- Reading/frontend: rich rendering and conversion preserve targets; main sequence vs TOC; note return and no auxiliary progress overwrite; image toggle performs no image requests; stale revisions reject reads/writes/resources; TXT/BookSource version-1 content remains readable.
- Race tests for changed scheduling/store/restore boundaries; focused normal tests first. Frontend typecheck/build and relevant component tests. One composed real-server journey for each intake channel, desktop/mobile reader inspection, no deployment/load claim.
- No universal EPUB conformance suite, benchmark project or large random corpus unless specific failures justify them. Optional legally supplied local EPUBs belong in repository-root `test-epubs/`, excluded from Git and Docker build contexts; they are compatibility evidence, never clean-checkout or CI dependencies. The user-supplied development example is `test-epubs/全职高手 (蝴蝶蓝).epub`; see Verification for the limited inspection performed. Real-book checks must be explicit local runs; default tests use synthetic archives. Do not commit extracted book content, images or generated reports containing substantial book text.

## Current State

Checkpoint 1 is partially implemented; no EPUB routes or publication capability are exposed.

- Dependency gate: isolated Readium v0.16.0 (`e36e46c88ad82c2ff9bfabc5657d988bd8c99cd4`) build succeeded on Go 1.27.0 linux/amd64. A caller doing only `epub.NewParser(nil)` had 790 compiled packages, 562 non-standard, 69 distinct modules including the proof module, and 310 cloud/AWS/gRPC/telemetry packages. Unstripped executable: 48,146,598 bytes. These are build-footprint observations, not runtime parsing benchmarks. No PDF-native dependency was observed in this import graph. The user explicitly selected focused Go implementation instead; no bounded Readium fetcher was built after this decision.
- Reproduction remains isolated from the application: create a temporary module, `go get github.com/readium/go-toolkit/pkg/parser/epub@v0.16.0`, compile `package main; import "github.com/readium/go-toolkit/pkg/parser/epub"; func main() { _ = epub.NewParser(nil) }`, then inspect `go list -deps -json .`. The local experiment and graph are in ignored `reference/epub-parser-proof/`; no upstream code or dependency was added to the application.
- `backend/internal/epub/` now owns bounded ZIP indexing/metadata reads, local URI resolution, bounded cancellable XML token decoding (including UTF-16 BOM and fixed HTML entities), and container/OPF inventory inspection. It preserves spine order, linear/non-linear membership, navigation IDs/properties and fallback IDs without treating TOC order as reading order. `Inspect` explicitly does **not** return a prepared/publishable book.
- Navigation inspection now reads EPUB2 spine-declared NCX `navMap/navPoint` or EPUB3 manifest-declared nav `epub:type="toc"` lists. It preserves document-order hierarchy, grouping headings, mixed inline labels and distinct path/fragment targets independently of spine. EPUB3 nav and legacy NCX are never merged; missing/invalid EPUB3 nav is diagnosed rather than silently substituted with NCX. XML namespaces are checked; base overrides are diagnosed as invalid optional navigation instead of ignored or fetched.
- Navigation uses the existing metadata/XML/context limits plus a 10,000-entry limit. Cancellation/limits and archive read failures remain fatal. Missing, malformed, empty or ambiguous optional navigation produces code-only diagnostics. Nonlocal, escaping, absent or nonmanifest targets retain label/hierarchy but no actionable target and are marked unavailable; missing labels have their own diagnostic. No target content or external DTD is fetched. A separate target-binding pass now checks fragments against registered normalized sections and emits section/opaque-anchor targets. Preparation orchestration now discovers and admits sections and supplies an explicitly labeled spine-derived list for empty navigation.
- Package support checks now reject declared fixed layout (EPUB 3 rendition vocabulary, including aliases and spine overrides; legacy fixed-layout metadata and Apple/Kobo display options). Encryption declarations permit only known IDPF/Adobe font obfuscation on declared font resources, which remain unused; encrypted non-font resources and other encryption are unsupported. No decryption or publisher-font rendering is implemented.
- Every spine item, including non-linear items, resolves through declared media fallbacks to an existing XHTML resource. Missing references/files, cycles and unsupported terminal media fail instead of silently dropping content. `SpineItem.ContentID` records the selected item while retaining original identity, order and linearity; fallback use has a code-only diagnostic. Multiple manifest IDs may alias one path when their media declarations agree; conflicting media declarations are rejected. The local example exposed why rejecting all aliases was unnecessarily strict; the fix applies to resource identity generally, not that book's IDs.
- `NormalizeSection` now converts one bounded XHTML document to a finite semantic tree: headings, paragraphs/groups, emphasis/strong/strike, sub/superscripts, code/preformatted text, lists, tables, ruby, line breaks, note roles and image/link bindings. It preserves language/direction and whitespace semantics. Block containers, inline groups, figures and figure captions now remain distinct so the renderer need not infer flow from surrounding nodes. The shared bounded XML tree now has its own owner (`xml_tree.go`) rather than duplicating navigation parsing. Styles/scripts and arbitrary publisher attributes are not emitted; unsupported meaningful content retains fallback text plus code-only diagnostics.
- Normalized anchors are opaque and separate from original IDs; ambiguous original IDs cannot resolve. Internal links and images use section-local opaque binding keys with private source-reference maps. HTTP/HTTPS external links are explicit-action candidates only; other schemes and remote images are inactive. A narrow SVG wrapper containing one image can yield a pending image binding; this is not SVG rendering or asset validation. Base overrides fail rather than silently redirect content. Empty readable content fails.
- This is an **internal preparation model**, not the frozen version-2 wire contract. It remains under `internal/epub` until the renderer proof establishes the final shared shape; existing version-1 endpoints are unchanged. No unvalidated binding is exposed for reading. Link/anchor binding over a completed section index, image-byte validation and per-section readability reassessment are implemented as described below. Staged preparation now connects these passes, discovers declared auxiliary documents and enforces admission before emitting bound sections.
- `backend/internal/reading/epub_document.go` now proves an explicit preparation-to-reading projection. The candidate version-2 shape retains the content envelope and `document.blocks`, with recursive prose nodes, leaf text, block/inline/figure semantics, and local targets containing `chapterIndex`, `contentRevision` and an optional opaque anchor. Qualified targets remain tied to their displayed revision even for delayed renderer events. Image references are issued by a caller-owned application callback using only section-local opaque keys; validated media type/dimensions are copied from binding evidence, never from archive paths or arbitrary publisher attributes. Unsupported source content retains safe fallback prose. Unknown node kinds, missing validated image bindings and cancellation fail rather than emit partial content. Mutable node/target/list containers do not alias canonical preparation state. Version-1 serialization, including empty block arrays, is unchanged. No live provider invokes this projection: the frontend/navigation proof now passes, while remaining profile/resource checks and durable integration still gate exposure and contract freeze. Reverting this unexposed increment needs no data conversion.
- Frontend candidate contract/renderer proof is implemented in `api/structured-prose.ts`, `features/reader/prose-nodes.ts` and the existing `ProseRenderer.vue`. The explicit parser admits finite nodes, bounded depth/node count, coherent revision-qualified targets, HTTP(S) external actions and same-origin application resource paths; it excludes publisher properties and rejects malformed leaf/action/resource shapes. A client-only `structureVersion` discriminator keeps legacy documents unambiguous. Legacy paragraphs/images adapt into the same renderer vocabulary; no second EPUB renderer or raw-HTML insertion was added. Structured documents retain authored headings and expose metadata title as an accessible article label, avoiding a duplicate generated heading. Application-owned elements preserve flow, lists/tables/ruby, language/direction and image dimensions; wide tables scroll locally. Failed images retain alt text; hiding structured images does not remove authored captions/other figure prose. Anchors are renderer-scoped data attributes, not publisher/global DOM IDs. Internal links require a caller-owned URL issuer and emit `{target, note}` with the original revision and explicit `noteref` intent, including notes within main sections; without an issuer they are non-actionable. External links require user activation and isolate the opener/referrer. Display conversion visits nested text/alt values without changing canonical trees, target identifiers or URLs. **`getChapterContent` now admits versions 1 and 2 through their separate strict parsers**, following the shared Go wire-fixture and composed ReaderView browser proof. The shared session now accepts both typed document shapes internally, with candidate version-2 loader/conversion reuse and note/anchor lifecycle integration tested below. Backend EPUB responses remain unexposed pending durable provider/resource integration.
- Optional catalog `auxiliary` membership is implemented across shared reading types and frontend parsing. The unregistered `epubCatalog` projection preserves prepared section ordinals and emits the flag only for auxiliary sections; legacy wire shape is unchanged. The unregistered projection now includes a separate optional `navigation` tree with provider-neutral provenance (`publication` for authored EPUB 2/3 TOCs, `sections` for preparation's explicit spine fallback). Entries retain labels, grouping-only nodes, unavailable targets and children, and use the same revision-qualified ReadingTarget as prose links. TOC ordering/repeated section targets do not change the section inventory or main sequence. Legacy providers omit navigation entirely. `api/catalog-navigation.ts` validates finite bounded trees and target membership/revisions; `getCatalog`/`waitForCatalog` retain this evidence. Reader and Book Detail now consume it through the user-selected always-expanded outline (`TocNavigationList.vue`), preserving the incumbent visual style, semantic nested lists, inactive group/unavailable labels, ancestor-preserving search, current-section focus and authored order (no reverse control). Pure TOC helpers own search/highlighting/text mapping; the list only emits existing ReadingTargets or builds qualified reader links. ReaderView stores canonical/display catalog navigation separately from transient visit state and routes selections through its existing anchor/note lifecycle; conversion caches labels by catalog/mode without changing targets or canonical text. Legacy catalogs retain their flat chapter list and controls. Shared location validation permits auxiliary bookmark locations but rejects them for progress before the library write. Frontend ordinary previous/next, prefetch and saved-resume fallback use main membership; explicit auxiliary locations remain reachable. ReaderView neither mutates its local main resume nor queues progress when auxiliary content is displayed. Existing providers still emit main sections only; there is no live EPUB adapter. Untitled-section bookmarks now use successful revision-qualified location lookup rather than title presence as identity: the shared service preserves empty title metadata, while the bookmark sheet displays the existing one-based section number convention. No synthesized title is persisted.
- `features/reader/reader-navigation.ts` proves revision-bound immutable transition proposals: nested note returns restore one captured position at a time, including same-main-section notes; note visits disable ordinary progression/prefetch and main writes. Explicit main links/chapter navigation leave note context. Departure captures actual normalized position, not an obsolete entry anchor. Failed/unapplied proposals cannot consume the old trail. This model is now wired into ReaderView: document/index/navigation commit together under its request generation; scoped anchor restoration awaits layout before progress can be queued, and a missing anchor rolls back the previous document, trail and position. Nested Return, refresh and direct `note=1` visits remain write-free, including same-main-section notes. The existing footer exposes Return one step at a time; main counters exclude auxiliary sections and note visits display a localized label. Note detours leave the origin route intact; issued anchor links carry revision and explicit note intent for new tabs/reloads. Ordinary main navigation clears note context and updates the qualified route. Bookmarks/progress cannot capture an in-progress scroll restoration. Renderer link clicks and locally scrolling tables retain their interactions rather than triggering reader keys/taps. Per-book state remains transient, not cross-book authorization or persisted history. Earlier integration tests inject structured documents at the loader seam; the composed browser proof now exercises actual version-2 transport parsing with mocked API responses.
- `targets.go` / `target_bindings.go` register copied path/anchor metadata without retaining section trees, then resolve forward/back/self links and hierarchical navigation over the completed index. Targets contain only an interpretation-local ordinal and opaque anchor; missing/ambiguous fragments never fall back to document start. Broken links retain text and an unavailable marker; external HTTP(S) actions remain untouched. Multiple spine occurrences of one resource keep self-links local, while ambiguous cross-document/TOC targets remain unavailable rather than selecting an arbitrary occurrence. Original IDs/paths do not enter resolved trees/navigation. Registration limits are 15,000 sections (spine plus manifest bounds), 250,000 anchor entries and 32 MiB of copied path/anchor text, plus bounded map overhead; cancellation/limit failure leaves registration unchanged. These are internal proof limits, not a frozen wire or persistence contract. The index does not discover/admit sections, infer fragment equivalence across fallback resources, choose main reading order or qualify targets with revisions; those remain orchestrator/reading-boundary responsibilities.
- Static image validation now uses Go's JPEG/PNG decoders: declared format must decode, dimensions are checked before bitmap allocation, and full pixel decoding detects corrupt payloads that header inspection would accept. Initial limits are 16 MiB encoded bytes, 16,384 per dimension and 32 Mi pixels. APNG is rejected because the standard decoder validates only its default image; GIF/WebP/SVG and other formats are explicit unsupported-image diagnostics, not presumed safe. No new dependencies or image transformations were added. The user selected orientation-aware display dimensions while retaining unchanged image bytes, rather than introducing transcoded derivatives. Encoded matrix dimensions still enforce decoding budgets; `image_orientation.go` reads only the primary EXIF orientation (JPEG APP1 and PNG eXIf), swapping display axes for orientations 5–8. Missing/malformed metadata has no orientation; no thumbnail-directory traversal or other EXIF data is retained. The actual renderer reserves stable before/after-decode geometry with those dimensions in the recorded desktop/mobile Chromium check.
- `Prepare` now owns the bounded parser orchestration: one archive index and actual-read budget, caller-owned empty disposable seekable scratch, then one bound section per callback after admission/target discovery. It seeds every spine slot first (including non-linear slots), discovers only existing declared XHTML from TOC/internal links, and terminates note cycles by manifest path. No remote fetch or arbitrary manifest sweep occurs. Main membership is explicit and is not inferred from an ordinal prefix. Missing navigation yields `Source=spine` with main-only entries; unavailable individual targets retain their diagnostics. Private validated image references/media evidence stay separate from presentation trees. Caller owns scratch cleanup and must discard the entire output attempt on any preparation, cancellation or callback error; no partial `Preparation` result is returned. This is not an alternative durable store or a publication transaction.
- Embedded cover selection is implemented in preparation. EPUB 3 `cover-image` declarations take precedence over legacy OPF `<meta name="cover" content="…">`; same-resource aliases are one choice, but conflicting declarations (including a known plus unresolved ID) yield `cover_image_ambiguous` and no selection. If a non-conflicting declaration is missing/unusable, an unambiguous validated image from an explicitly identified cover page may be used, retaining degradation diagnostics. Page evidence is captured before image validation removes bad bindings, so a surviving decorative logo cannot become a guessed cover. Repeated references to one resource are not competing artwork. Cover-only pages outside the reading graph are inspected for artwork without adding sections or following their links. No filename, ordinary-chapter or dimension heuristics; no whole-manifest content sweep. Absent artwork is represented by `Preparation.Cover == nil` for the existing default-art fallback. `Cover` contains private validated `PreparedImage` evidence, not a public URL, copied asset or new storage/customization owner. Fatal read/limit/cancellation errors still abort preparation before emission.
- Accepted cover admission is implemented: an explicitly identified cover that becomes unreadable retains its section position with `CoverPlaceholder` and review diagnostics only if some main content remains readable. Recognition uses body-level EPUB cover semantics, whole-document OPF guide references or EPUB 3 cover landmarks—not filenames or image-only heuristics. A fragment landmark cannot exempt an entire mixed-content document. Other unreadable/malformed spine sections fail, including explicit non-linear spine entries; malformed/unsupported/unreadable optional referenced XHTML remains unavailable with diagnostics. Cancellation, limits, ZIP/read corruption and scratch/output failures are never softened into optional warnings.
- Staging budgets: 64 MiB JSON per section, 2 GiB total scratch and 4 MiB aggregate section-title text, in addition to existing input/index/image bounds. One normalized tree is serialized/read at a time; anchors live in the bounded target index rather than being copied into scratch. JSON serialization still allocates a per-section buffer; these are not zero-copy/constant-memory claims. OCF names and decoded URI fragments now reject invalid UTF-8 at the input boundary so JSON cannot replace bytes and silently retarget a link.
- A preparation-local image resolver checks only declared, referenced archive resources. It retains bounded validation metadata by manifest path, not decoded pixels/bytes, so repeated illustrations are validated once. Valid image nodes receive dimensions; missing/invalid/unsupported images become non-actionable placeholders with alt text and code-only diagnostics. Cancellation, ZIP/read corruption and size-limit errors remain fatal. Readability is recomputed after binding; it is evidence for whole-book admission, not an automatic failure for a missing cover or permission to publish an empty book. Preparation emits validated image metadata/private references, not image copies; no image endpoint or durable prepared-asset persistence exists yet.
- Section input is capped at 16 MiB and shares the XML depth/token bounds. Archive reads enforce actual bytes cumulatively (up to 1 GiB for content), rather than trusting declared sizes. No section segmentation/virtualization was added. Ordered-list starts retain signed 32-bit semantics; table spans outside the initial 1–1000 range are diagnosed and omitted. These preparation limits/profile still need the renderer/cache-size proof before freezing.
- Initial inspection limits: 256 MiB compressed archive, 20,000 ZIP entries, 1 GiB declared expanded size, 4 MiB per metadata read, 16 MiB cumulative metadata read budget, XML depth 128 / 200,000 tokens, 10,000 manifest items and 5,000 spine items. These are internal limits, not settings. Standalone inspection does not decompress chapters/images; `Prepare` reuses that same archive and includes metadata, section and image reads in the cumulative content budget.
- The optional local example passes inventory/navigation inspection (16,778,888 compressed bytes, 1,760 manifest items, 1,737 spine entries, 1,736 navigation entries, no navigation or support diagnostics). The explicit normalization/image check processed all 1,737 spine sections, one at a time: 18,359,861 XHTML input bytes, 25,582,370 semantic-tree JSON bytes, 3,472 valid image bindings from 16 distinct resource checks, zero unreadable sections/local link bindings/anchors, and no diagnostics in 2.27 seconds locally. Target binding also resolves all 1,736 navigation entries, with none unavailable. This is elapsed compatibility-test time, not a throughput benchmark or performance comparison. Unreferenced assets were not decoded. This book does not exercise internal-link/anchor compatibility; synthetic tests do. Target fragments are tested synthetically, not by this local book. The subsequent full staged preparation proof also passes for this example (measurements below). Embedded cover selection now also passes the staged local-book proof. Authorized resource serving and actual local-book browser reading remain unvalidated; the synthetic shared-renderer browser proof below is not full-book compatibility.
- No external dependencies, reader schema or existing homes/backups changed. Frontend changes are the shared renderer/conversion and candidate contract proof, not EPUB import or live reading exposure. Untracked frontend prototypes remain untouched.

- Cover placeholders now survive prepared-section emission and version-2 projection as an optional `document.coverPlaceholder` flag. The user selected existing default artwork with a localized “Original cover unavailable” caption. ProseRenderer reuses BookCover and retains the semantic tree/anchors; the substitute and caption are application presentation, never injected book text or a resource binding. Disabling images hides the artwork but retains the caption. The flag is absent for ordinary/legacy documents; admission still requires an explicitly identified cover and other readable main content.

## Next Action

Continue with auxiliary bookmark reopen behavior and supported-profile finalization before freezing the contract. Cover-placeholder presentation and representative normal/large-section payload/cache checks now pass; parser-limit-sized documents and physical low-memory/mobile-device performance were not measured. Preparation measurements do not establish browser or worst-case image-memory performance. Review EPUB 2 primary references and finalize practical limits/error categories; do not add speculative splitting or a separate cache owner.

The shared Go-to-frontend wire contract and composed ReaderView note/TOC/table/image-toggle checks now pass. Client transport admits version 2 through its strict parser; current backend providers still emit version 1. Do not duplicate the reader state model or infer reading order from the TOC. Browser APIs were mocked, so authorized resources, real persistence/last-read protection, backup/removal and complete EPUB journeys remain unproved. Renderer href issuance is not authorization.

Confirm fresh-data schema cutover before checkpoint 2. Scratch/output lifecycle remains caller-owned until `epubstore` integrates durable generation/recovery rules; existing homes/backups remain untouched. Do not reopen dependency selection without new evidence.

## Verification

Recorded checks (backend commands run from `backend/`, frontend commands from `frontend/`):

- Cover placeholder: EPUB/reading package tests pass, covering emitted marker ownership and anchor-preserving projection without invented prose/resources. Targeted renderer/parser/conversion/i18n tests pass (4 files / 18 tests), production build/typecheck and scoped zero-warning ESLint pass. Actual renderer browser checks at 1280/390px pass for existing artwork, localized caption, retained anchors, hidden-image caption-only state and no overflow/page errors; ignored evidence is `reference/epub-cover-proof/`. AFT inspection could not complete due to transport failure; compiler/tests are authoritative. No schema, provider exposure or existing-data change.
- Representative payload/cache measurement (Chromium 152, this host, Vite dev build, mocked transport): 100 rich paragraphs = 38,720 wire bytes / 401 semantic nodes; 5,000 paragraphs = 1,939,020 bytes / 20,001 nodes. At 1280/390px, cold fetch/parse took 4–7 ms / 61–76 ms respectively; mount plus two animation frames took 14–26 ms / 258–268 ms. Warm load reused object identity in 0–0.1 ms without another request. Loading six sections evicted the oldest from the existing five-entry cache, and revisiting it fetched again. After forced GC, five large cached sections increased JS heap by ~16.9 MB over pre-run baseline; disposal reduced that to ~5.3 MB (includes runtime/render/JIT overhead, not a retained-cache size claim). No evidence here justifies a new cache owner, byte-budget tuning or section splitting. This is one synthetic run per size/viewport, not a physical mobile benchmark, production latency budget, decoded-image memory check or parser-cap worst case. Reproducible ignored harness/raw results: `reference/epub-payload-proof/`.

- Image orientation check reproduced and resolved a layout defect with a synthetic 480×240 JPEG carrying EXIF orientation 6. Actual ProseRenderer in Chromium reserved 480×240 before decoding, then rendered 480×960 (natural oriented dimensions 240×480). At 390px viewport width it shifted from 374×187 to 374×748. An otherwise identical unrotated JPEG retained its geometry. `ValidateImage` now supplies orientation-aware display dimensions. Using actual Go-validated fixture dimensions, both rotated JPEG and PNG reserve and display 240×480 without a load-time geometry change at desktop/mobile widths; the plain JPEG remains responsive at 480×240 / 374×187. `go test ./internal/epub ./internal/reading -count=1` passes, including both EXIF byte orders, orientation-axis groups and truncated/out-of-range metadata. No frontend or dependency change. AFT completed but has no Go diagnostic producer (`gopls` unavailable); Go tests are authoritative. Local synthetic evidence/harness: `reference/epub-image-proof/`. This is a measured geometry check, not a performance benchmark.

- Structured transport/composed-reader checkpoint: `testdata/epub-reading.json` is checked against actual synthetic EPUB preparation and Go catalog/content projection, then consumed through frontend `getChapterContent`. It covers the main/auxiliary inventory, note target/anchor coherence, image media/dimensions, zero-based list start, table spans and fallback prose. `go test ./internal/reading -count=1` passes; frontend reader/API tests pass (22 files / 84 tests), production build/typecheck and scoped zero-warning ESLint pass. Unknown versions and malformed version-2 nodes fail without legacy fallback. At 1280×900 and 390×844, real ReaderView/loader/renderer with mocked API responses pass keyboard note activation, anchor focus, nested return and actual scroll restoration, TOC auxiliary selection, table keyboard/pointer isolation, no auxiliary progress requests, warm section reuse, hidden-image zero requests and explicit image display, with no horizontal overflow/page errors. Final run uses production modules without the preliminary parser hook. Ignored reproducible harness/evidence: `reference/epub-reader-proof/`; its catalog/prose extend the shared synthetic wire fixture for long scrolling, wide tables and nested visits. This is not a real backend, authorization, bookmark-persistence, last-read or throughput proof. AFT inspection timed out; compiler/tests are authoritative.

- TOC presentation increment: affected frontend tests pass (22 files / 83 tests), including shared-list search/qualified targets/inactive states, Book Detail propagation, ReaderView auxiliary selection/return and main-progress isolation, and canonical-text-preserving cached label conversion. Production build/typecheck and zero-warning scoped ESLint pass. Synthetic Playwright checks at 1280×900 and 390×844 pass for Book Detail links, expanded groups/unavailable ancestors, long labels, search, keyboard activation, centered current-row jump, Escape, and no horizontal overflow/page errors. A fresh Vite process was required because WSL file watching served stale modules; the final browser run checks the new section-summary copy explicitly. Ignored evidence is under `reference/epub-toc-proof/`. No backend/schema/data changes or live EPUB admission in that increment; the later composed ReaderView proof is recorded above. AFT diagnostics did not complete (transport timeout); compiler/test checks are authoritative.
- Catalog navigation increment: the shared synthetic wire fixture `backend/internal/reading/testdata/epub-navigation.json` is checked against Go EPUB 2/3 projection and consumed through frontend catalog transport. Focused `go test ./internal/reading -count=1` passes; frontend parser/API tests pass (3 files / 12 tests) plus typecheck. Covers hierarchy, repeated section anchors, authored order distinct from spine order, unavailable parents with reachable children, optional legacy serialization, fallback provenance, stale/missing/volume/conflicting targets, and depth/count limits. Shared ReadingTarget parsing now owns the revision rule for both prose links and navigation; no private preparation types/paths cross this contract. AFT timed out; compiled/tests checks remain authoritative. Broader affected-area verification also passes: `go test ./internal/reading ./internal/api -count=1`; frontend reader/catalog/BookDetail tests (22 files / 83 tests), production build/typecheck and zero-warning scoped ESLint. No browser, live provider, schema or existing-data change in this increment.
- Reader lifecycle increment: targeted frontend tests pass **21 files / 78 tests** across reader, reader/structured API and i18n; production build/typecheck and scoped ESLint pass. Coverage includes nested same-main notes, refresh write exclusion, failed/superseded proposals, scoped anchor restoration with rollback, direct note visits, qualified link intent, mixed legacy/structured conversion-cache reuse and focused link/button/table keyboard behavior. The final navigation/i18n rerun also passes (2 files / 20 tests) after focus restoration and compact Return labels. Geometry/frames are controlled in the integration fixture; this is not a real-browser note workflow. No backend tests or new browser run for this frontend increment. AFT timed out; TypeScript/Vitest/build are authoritative.
- Untitled-bookmark correction: reproduced HTTP 400 for a valid untitled section, then passed `go test ./internal/api -run '^TestBookmarkAPI' -count=1` and `go test ./internal/reading ./internal/library -count=1`. The regression covers exact repeated-ID retry, stored location/empty title, volume/missing-section rejection and unchanged resume/last-read with only one state-version increment. Frontend bookmark-sheet tests pass (1 file / 2 tests), plus typecheck and scoped ESLint; the new case verifies numeric display fallback and exact revision-qualified reopen event. No browser, build or additional race run for this localized correction. AFT timed out; compiled/tests checks are the verification authority.
- Membership/navigation increment: `go test ./internal/reading -count=1` and `go test -race ./internal/reading ./internal/api ./internal/library -count=1` pass. Covers prepared inventory projection, exact legacy catalog serialization, main/auxiliary progress-versus-bookmark location validation and existing API/library behavior. Frontend targeted Vitest (`src/features/reader`, reader/structured-prose API tests and BookDetailView) passes **21 files / 72 tests**, production build/typecheck and scoped ESLint pass. Covers malformed/duplicate catalog membership, interleaved auxiliary skips, auxiliary ReaderView no-write/prefetch/bookmark capture, nested same-main/auxiliary note return proposals and stale/unavailable targets. No new browser or full EPUB lifecycle check was run for this increment. AFT again timed out during Tier-2 analysis; compiled/tests results are authoritative.
- Frontend `npm run typecheck`, `npm test -- src/features/reader src/api/reader.test.ts src/api/structured-prose.test.ts` (19 files / 61 tests), production build and scoped ESLint pass for the candidate parser, common renderer and conversion. Coverage includes finite safe DOM/text, unchanged legacy rendering, revision-qualified note actions, scoped anchor lookup, image failure/hiding, immutable text conversion and resource/action/depth rejection. The style detector returned no scoped findings. The latest AFT inspection timed out during Tier-2 analysis; TypeScript/Vitest/build are authoritative here.
- Isolated synthetic Vite/Playwright proof passed at 1280×900 and 390×844: no horizontal page overflow or page errors, wide tables scroll locally, literal script-like text remains text, block flow is preserved, PNG reserves/renders 4:3 at 640×480 and 342×256.5 respectively, and Enter activates a revision-qualified note event. Resource delivery was intercepted with a generated PNG; no reader account/data or backend EPUB endpoint was used. Screenshots and the local harness are ignored under `reference/epub-renderer-proof/`; the temporary frontend entry is removed. The CLI Chromium launch failed in crashpad; the successful proof used the installed Playwright headless shell. This is not note-return, authorization, EXIF-orientation, cold/warm cache or large-section performance proof.
- `go test ./internal/reading ./internal/epub -count=1` and `go test -race ./internal/reading ./internal/api ./internal/epub -count=1`: preparation-to-reading integration with main/note membership, block versus inline flow, figure/caption and ruby/list/table semantics, language/direction, revision-qualified note targets, explicit external/unavailable links, opaque image references with validated media/dimensions, safe unsupported-content fallback, canonical list-state isolation, private-data exclusion, missing-binding/unknown-kind/cancellation rejection and exact version-1 wire compatibility. Existing API regressions also pass. This is backend contract evidence, not a renderer or authorized-resource proof.
- `go test ./internal/epub -count=1` and `go test -race ./internal/epub -count=1`: synthetic package/order/reference cases, archive and XML boundaries, cancellation, EPUB 2 declarations/entities/UTF-16, NCX/nav hierarchy and selection, mixed labels/group headings, repeated-document fragments, spine independence/non-linear targets, unavailable targets, optional-navigation diagnostics, base overrides, fatal navigation byte/depth/entry/cumulative limits, fixed-layout declarations, font obfuscation versus encryption, spine fallback/cycle/missing-file handling, consistent versus conflicting resource aliases, semantic structure/whitespace, opaque/private reference separation, safe/inactive links/images, ambiguous anchors, SVG raster wrappers, bounded section normalization, complete static-image validation versus false declarations/corrupt pixels, animation/dimension/byte limits, repeated-resource deduplication and readability after missing images, forward/self/note-return targets, literal decoded-fragment matching, unavailable/ambiguous targets without fallback, resolved-tree/navigation privacy, repeated-resource occurrence handling and target-index ownership/limits/cancellation, full staged preparation, interleaved non-linear spine membership, cyclic auxiliary discovery, unused-resource exclusion, bound image metadata across staging, explicit cover declarations/placeholder admission, unreadable primary-content rejection, fatal auxiliary limits, output/cancellation/occupied-scratch/short-write failures, stage budgets, large-section completeness, UTF-8 reference integrity, cover declaration precedence/alias/conflict handling, explicit-page fallback without reading-graph changes, prevention of surviving-decoration misselection and fatal cover-validation failures. Default runs skip the explicitly opt-in local fixture.
- `NOVELREADER_EPUB_FIXTURE="$(pwd)/../test-epubs/全职高手 (蝴蝶蓝).epub" go test ./internal/epub -run '^TestLocalPackageInspection$' -count=1 -v`: package support/inventory/navigation only; logs counts and diagnostic codes, not title/text/hrefs/assets. Use an absolute fixture path because Go tests run in their package directory; an earlier relative-path invocation failed before reading the book.
- `NOVELREADER_EPUB_FIXTURE="$(pwd)/../test-epubs/全职高手 (蝴蝶蓝).epub" go test ./internal/epub -run '^TestLocalSectionNormalization$' -count=1 -v`: full spine section normalization, referenced static-image validation and navigation binding against the completed target index; logs counts/diagnostic codes, not book content or original references. Sections are not retained together. The target proof requires readable sections; it does not guess cover-admission policy or silently omit unreadable spine entries.
- `NOVELREADER_EPUB_FIXTURE="$(pwd)/../test-epubs/全职高手 (蝴蝶蓝).epub" go test ./internal/epub -run '^TestLocalPreparation$' -count=1 -v`: full staged parser preparation, emitting/counting one tree at a time; repeated successfully after the flow-semantics correction. Recorded cover-selection checkpoint (`b269f15`) measurements: 1,737 main sections, zero auxiliary sections, 3,472 image bindings, 25,582,370 tree JSON bytes, 25,921,003 scratch bytes, an embedded cover selected and no diagnostics (2.40 seconds at that checkpoint). Links/auxiliary behavior is synthetic-test coverage, not evidence from this particular book.
- Focused process measurements recorded at staged-preparation checkpoint `97cdb41` (before cover selection): compiled a non-race `go test -c` binary and ran each proof in a fresh child, using Python standard-library `os.wait4` peak RSS and wall time (system `/usr/bin/time` is unavailable). Go 1.27.0 linux/amd64: normal synthetic staged proof **7,488 KiB / 0.010 s**; 1.17 MB, 10,000-paragraph single-section proof **31,276 KiB / 0.088 s**; local-book staged proof **24,464 KiB / 2.415 s**. These include test/runtime and synthetic fixture construction, not isolated parser heap measurements or a comparative benchmark. The local callback also serializes emitted trees for byte counts. The local book repeats small raster resources; this does not measure maximum-sized image decoding or browser layout/cache costs.
- `go test ./internal/epub -run '^$' -bench '^BenchmarkNormalizeSection$' -benchmem -count=1`: normal synthetic section 18,551 ns/op, 17,968 B/op; 1.1 MB / 10,000-paragraph single document 31,480,811 ns/op, 28,496,645 B/op, on the same Go/CPU below. Construction excluded; allocations are not peak memory, and this excludes image decoding, target binding, persistence and rendering.
- `go test ./internal/epub -run '^$' -bench '^BenchmarkInspect$' -benchmem -count=1`: synthetic metadata inspection with an approximately 19 MiB single-spine body took 46,485 ns/op, 26,295 B/op, 376 allocations/op on AMD Ryzen 9 5900X, Go 1.27.0 linux/amd64. Fixture construction is excluded; chapter bytes are not decoded. This is not peak memory, full preparation, rendering performance or a comparison with Readium.

Navigation implementation consulted the EPUB 2 OPF NCX requirements at <https://idpf.org/epub/20/spec/OPF_2.0.1_draft.htm> and EPUB 3.3 navigation restrictions/TOC definition at <https://www.w3.org/TR/epub-33/>. Earlier reference-reader research is linked above. The earlier backend scoped AFT inspection completed, but lacked authoritative Go diagnostics because `gopls` is unavailable. Focused Go tests/race compilation are the authority. The metadata benchmark above predates navigation/support parsing and was not rerun for this slice.

Not performed: authorized cover/resource serving and shelf integration, worst-case image-memory/browser-cache measurements, integrated EPUB reader/browser validation, end-to-end auxiliary bookmark/resume integration and browser-confirmed same-main note UI write isolation, durable EPUB storage/backup/restore, schema cutover, complete local-book compatibility, container build or hosted CI. Checkpoint 1 remains unfinished.
